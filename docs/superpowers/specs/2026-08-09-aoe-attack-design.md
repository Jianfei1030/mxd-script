# 群攻 —— 接敌区内怪数达阈值时改用双向技能

日期：2026-08-09
状态：设计已确认，待实现
上游：`2026-08-07-directional-attack-zone-design.md`（接敌区/攻击区两分，本设计的计数区就是它的接敌区）、`2026-08-06-attack-zone-mob-gating-design.md`（攻击区与锚点）

## 1. 问题

现在只有一条输出路径：`_do_attack`（`MapleFarmTask.py:827`）按 `攻击间隔` 轻点唯一的 `攻击键`，打的是**有向攻击区**（接敌区的面朝侧一半，`farm_logic.facing_half_zone`）。

被怪围住时这条路径有两个结构性损失：

1. **背侧的怪打不到。** 有向攻击区只覆盖面朝侧，背后那一半怪只能靠转向去打，而转向受 `转向冷却(秒)`（默认 1.5）限制 —— 与默认 `攻击间隔(秒)=1.5` 齐平，用户实机把攻击间隔调到 0.7 时更是长一倍（实跑配置见 `2026-08-07` spec §3.1）。
2. **转向本身在消耗节拍。** 区内两侧都有怪时，`attack_turn_direction` 的目标侧锁定只在面朝侧真空后才换边，但换边那一拍要付一次方向键 tap + `self._last_detect = 0.0` 作废检测节拍（`:694`）。怪多的时候这种拍不罕见。

冒险岛的群攻技能（前后双向命中）恰好对上这两条：它不需要朝向，一发覆盖两侧。

**注意 `攻击区形状=群体(对称)` 不是这个功能。** 它只是把有向区退回对称区，**按的还是同一个 `攻击键`** —— 那是有向攻击区改动的安全退路，不是「换技能」。群攻是一个独立技能槽、独立按键、独立节拍。

## 2. 现状盘点

| 事实 | 位置 |
|---|---|
| 单体攻击：`_last_attack_present` + `攻击间隔` 轻点 `攻击键` | `MapleFarmTask.py:827` |
| 接敌区（对称，`攻击区宽 × 攻击区高`）算出的地方 | `:606` |
| 有向攻击区 `attack_area` | `:611` |
| 转向轻点（含 `转向冷却` / `硬直抑制窗` / `_key_sendable` 三重门） | `:670-694` |
| `run()` 内顺序：`_detect_and_act` → `_do_attack` → `_do_seek_move`，**同一 tick 同一个 `now`** | `:1080` 一带 |
| 区内怪数已在 `_log_decision` 算过一遍（`in_zone`） | `:753` |
| 按键类配置（含「留空 = 不启用」约定） | `config.py:9-23` |
| 全局状态复位表 | `:212` `_reset_state` |
| GUI 下拉注册 | `:204` `_register_config_types` |

历史实测（`2026-08-07` spec §3.1，本次未重测，仅用于定阈值）：`800×200` 的接敌区里**平均区内怪 0.36 只、有怪帧占 28%、两侧都有仅 6%**。

## 3. 设计

一句话：**接敌区内怪数 ≥ 阈值且群攻键已绑、群攻节拍已到 → 按群攻键，那一拍不转向、也不按单体攻击键。其余一切照旧。**

### 3.1 配置变更

| 键 | 位置 | 默认 | 说明 |
|---|---|---|---|
| `群攻键(可留空)` | `config.py` 全局「游戏按键」 | `''` | 群攻技能键。留空 = 功能关闭 |
| `群攻怪数阈值` | 任务配置 | `3` | 接敌区内怪数达此值改用群攻 |
| `群攻间隔(秒)` | 任务配置 | `2.0` | 群攻自己的节拍，与 `攻击间隔` 独立 |

**留空 = 关闭，不另设开关键。** 与 `椅子键(可留空)` / `宠物食物键(可留空)` / `回城卷键(可留空)` 同一约定。

**阈值 3 的依据**：§2 的历史实测里「两侧都有怪」只占 6%，`平均区内怪 0.36`。阈值设 2 会让「一左一右两只」这种常见局面天天触发群攻 —— 那不是群攻要解的场景，转向一次就够。3 是「确实被围住了」的起点。阈值与间隔都是旋钮，实跑后按决策日志里现成的 `区内=N` 分布回调。

**为什么群攻有独立节拍**：群攻耗蓝通常是单体的数倍。共用 `攻击间隔`（默认 1.5s，实机常调到 0.7s）会瞬间见底，还会把喝蓝逻辑一起拖抖。

### 3.2 计数区 = 接敌区，不新增区

数**接敌区**（对称，`攻击区宽(像素) × 攻击区高(像素)`）里的怪，判定点仍是**框中心**（`point_in_zone`，与 `mob_in_zone` 同源）。

不新增 `群攻区宽/高`，理由同 `2026-08-07` spec §4.5「不新增射程参数」：群攻真实范围**从没标定过**，现在加两个旋钮只是把未知包装得更精致，还多两个要维护的参数。

且不同源会引入一类难查的状态：「群攻区里 5 只、接敌区里 2 只」——而转向/寻怪/坐椅/走位全都吃接敌区，两个区打架的排查成本远高于收益。等实跑出现「区内 4 只却打不到」，再用 `scripts/calibrate_attack_zone.py` 标出真值拆参数。

### 3.3 纯函数

```python
def crowd_present(mob_count, threshold):
    """接敌区内怪数达到阈值 → 该用群攻。threshold <= 0 视为关闭(恒 False)。"""
    return threshold > 0 and mob_count >= threshold
```

节拍复用现成的 `should_attack(now, self._last_aoe, cfg['群攻间隔(秒)'])`，不新写节流函数。

### 3.4 单一判据 `_aoe_ready(cfg, keys, now)`

转向门与攻击门**必须用同一个表达式**，否则会出现「以为要群攻所以没转向，结果群攻没发」这种两头落空的拍。所以抽成一个方法，两处都调它：

```
攻击模式 == '检测'
且 群攻键(可留空) 非空
且 farm_logic.crowd_present(self._last_zone_count, cfg['群攻怪数阈值'])
且 farm_logic.should_attack(now, self._last_aoe, cfg['群攻间隔(秒)'])
且 not farm_logic.stun_suppressed(now, self._last_hit, cfg['硬直抑制窗(秒)'])
```

`_detect_and_act` 与 `_do_attack` 在同一 tick 内被同一个 `now` 调用，期间没有任何代码写 `_last_aoe` / `_last_zone_count` / `_last_hit`，所以两次求值必然同值。这是「同一拍不转向 ⟺ 同一拍真发群攻」的正确性前提，实现时不许把其中一处换成别的写法。

### 3.5 接线

**`_detect_and_act`**（`:584`）：
- 算完 `zone`（`:606`）与 `centres` 之后，取区内怪（框中心落在 `zone` 内）写 `self._last_zone_count = len(in_zone)`；阈值判定留给 `_aoe_ready`
- 这个 `in_zone` 列表顺手传给 `_log_decision`，取代它现在自己重算的那一份（`:753`）——同一个数被算两遍是将来漂移的种子
- 转向门加一项：`if turn is not None and not self._aoe_ready(...) and turn_allowed(...) and ...`

**`_do_attack`**（`:827`）：群攻优先，二选一。

```
if self._aoe_ready(cfg, keys, now):
    send_key(群攻键); self._last_aoe = now; self._last_attack = now; return
<原单体逻辑一个字不改>
```

**新状态**（进 `_reset_state`，`:212`）：`_last_aoe = 0.0`（哨兵 = 从未群攻，天然放行）、`_last_zone_count = 0`（最近一次检测拍的接敌区内怪数，`0` = 还没检测过）。

存**计数**而不是布尔 `_last_crowd`：少一个要同步的状态；阈值在 `_aoe_ready` 里**用时现读**，GUI 改 `群攻怪数阈值` 立刻生效，不用等下一个检测拍；日志行（§4）也正好要这个计数。

### 3.6 群攻拍不转向

群攻双向命中，转向对它零收益，而代价是实打实的：一次方向键 tap + `self._last_detect = 0.0` 作废检测节拍（`:694`）。

**只在真发群攻的那一拍跳过转向**，群攻冷却中的拍照常转向、照常单体输出。不做「crowd 成立就整段禁转向」——那样 `群攻间隔` 那 2 秒里如果怪全在背侧，单体攻击区（面朝侧半区）是空的，角色会站着挨打。

副作用（是好事）：群攻拍 `_facing` **完全不动**，不会出现「群攻拍盲写了朝向、下一拍单体按着错朝向打空」这种新分叉。

### 3.7 群攻同时推进 `_last_attack`

群攻施法约 1 秒。`攻击间隔(秒)` **一旦被调到比施法时长短**（默认 1.5 不会，用户实机的 0.7 会），不推进单体节拍的话，群攻按下后一个攻击间隔单体攻击键就落下，**把自己的群攻打断** —— 与 2026-08-07「长按连挥 → 改回轻点」修的是同一类病（按键边沿与施法窗口打架）。推进后单体至少等满一个 `攻击间隔`。

默认配置下这一步是**惰性**的（1.5 > 1 秒，本来就不会撞上）。仍然要写，是因为它兜的是用户把节拍调快之后的情形，而那正是本项目实跑用的配置。

### 3.8 寻怪与群攻天然互斥，不写额外门

`crowd` 成立 ⇒ 接敌区内有怪 ⇒ `raw_present` 为真 ⇒ `seek_hold` 为真 ⇒ 走接战分支。所以「一边追一边放群攻」在结构上不可能发生，不需要新分支。

但这是个**隐式**不变量，要写一条测试把它钉死（§5.2 用例 i），否则将来有人改了 `seek_hold` 的算法就会静默破掉。

### 3.9 行为矩阵

| 接敌区怪数 | 群攻键 | 群攻节拍 | 行为 |
|---|---|---|---|
| ≥ 阈值 | 已绑 | 已到 | **按群攻键；不转向；不按单体攻击键** |
| ≥ 阈值 | 已绑 | 未到 | 照常转向 + 单体输出 |
| ≥ 阈值 | 留空 | — | 逐键等同改动前 |
| < 阈值 | 任意 | — | 逐键等同改动前 |
| 攻击模式 = 定频 | 任意 | — | 逐键等同改动前（没有找怪信息，无从计数） |

### 3.10 计数不去抖

YOLO 单帧 recall 0.886，4 只可能读成 3 只 —— 但这只会**漏触发一拍**（延迟 ≤ 一个检测拍），不会误触发。而 `群攻间隔` 2s 本就比一拍长得多。

反过来，加保持窗会把「被围」状态撑长：怪已清光却还按旧计数多放一发空群攻，浪费的是蓝。阈值本身就是旋钮，不需要再叠一层去抖。

（这与 `丢怪保持`/`寻怪保持` 的取舍方向不同，是因为那两处的失败代价是「停手/停步」，而这里的失败代价只是「晚一个检测拍放群攻」。）

## 4. 可观测性

**不动 `decision_log_line`。** 它是决策行格式的唯一事实源，`scripts/analyze_facing.py` / `scripts/analyze_seek.py` 的正则和一批绑定测试都吃它；为一个布尔值改格式不划算。区内怪数本来就有 `区内=N` 字段，阈值分布可以离线算。

**新增 `aoe_log_line(count, threshold)`**，每次**真按下群攻键**写一行（照「受击」那行的先例，同为格式唯一事实源）：

```
群攻 区内=N 阈值=M
```

判据直接 grep「群攻」。

**overlay**（`_draw_debug`，`:511`）：接敌区框在群攻就绪时线宽 `1 → 3`、标签由 `接敌区` 改为 `接敌区(群攻)`。这是判据 D 的视觉验收对象。

就绪标志由 `_detect_and_act` **求值一次**、作为新参数（`aoe_ready=`）传进 `_draw_debug`，与既有的 `mob_present=` / `attack_present=` 同一写法。不在 `paint` 闭包里再调 `_aoe_ready` —— 闭包是 Qt 重绘时才执行的，那时 `now` 已经过去，会画出与决策不一致的框。

## 5. 代码结构

| 文件 | 动作 | 内容 |
|---|---|---|
| `config.py` | 修改 | 全局按键加 `群攻键(可留空)` + `config_description` |
| `src/task/farm_logic.py` | 修改 | 新增纯函数 `crowd_present(mob_count, threshold)` |
| `src/task/MapleFarmTask.py` | 修改 | `DEFAULT_CONFIG` 加两键 + 说明文案；`_reset_state` 加 `_last_aoe`/`_last_zone_count`；新增 `aoe_log_line` 与 `_aoe_ready`；`_detect_and_act` 算 `_last_zone_count`、转向门加判据、`in_zone` 复用；`_do_attack` 群攻分支；`_draw_debug` 群攻态线宽/标签 |
| `tests/test_farm_logic.py` | 修改 | `crowd_present` 单测 |
| `tests/test_farm_task_offline.py` | 修改 | §5.2 全部用例 |

**热重载注意**：改了 `farm_logic.py` 必须重启 GUI（`ok/gui/tasks/TaskManger.py` 的 `importlib.reload` 不递归依赖）。改了 `config.py` 同样要重启。

### 5.1 纯函数单测（`tests/test_farm_logic.py`）

`crowd_present(mob_count, threshold)`：
- `(3, 3)` → True（**等于阈值算命中**）
- `(2, 3)` → False
- `(4, 3)` → True
- `(0, 3)` → False
- `(5, 0)` → False、`(5, -1)` → False（阈值 ≤ 0 = 关闭）

### 5.2 任务级离线测试（`tests/test_farm_task_offline.py`）

几何前提照既有约定：`DEFAULT_CONFIG['角色名']` 为空串 → 锚点回退画面中心 (1280, 720)、身体 (1280, 630)、接敌区 x∈[980,1580] y∈[530,730]（默认宽 600）。

- **a.** 区内 3 只 + 群攻键已绑 → 发群攻键，**不发攻击键**
- **b.** 区内 2 只 → 原样发攻击键，不发群攻键
- **c.** 群攻键留空 → 同一场景下 `send_key` 调用序列**逐键等同改动前**
- **d.** 怪在背侧 + 区内 3 只 + 转向冷却已过 → **不发方向键**；对照：同场景区内 1 只时照发
- **e.** 群攻间隔未到 → 不重发群攻键，且该拍照常走单体输出 + 转向
- **f.** 群攻发出后紧接的下一 tick 不发攻击键（`_last_attack` 已推进，§3.7）
- **g.** `硬直抑制窗 > 0` 且刚受击 → 不发群攻键
- **h.** `攻击模式 = 定频` → 不发群攻键
- **i.** 群攻成立那一拍 `_seek_dir is None`（§3.8 的隐式不变量）
- **j.** 决策日志开时含 `群攻 区内=N 阈值=M`，且 `N >= 阈值`

跑测试前确认 `screenshots/test_frames/` 是目录且含 `training_ground_full_2560x1440.png`（每次跑 GUI 都会被清掉，备份在 `_frames_backup/`）。

### 5.3 E2E + 实弹（AGENTS.md §11.3 要求）

**D（E2E 截图视觉验收，合入门槛）**：启 `main_debug.py` → 开 `启用标记框` + 检测模式 → 截图（`scripts/_e2e_capture.py <pid> <out>`）→ 存 `screenshots/e2e/aoe_attack/`。视觉模型需确认群攻就绪时接敌区框明显变粗且标签为 `接敌区(群攻)`。验收 FAIL 不许合入。

**A（确实生效且计数自洽）**：绑好群攻键，开 `决策日志开关` 挂机 10 分钟：
- grep「群攻」行数 **> 0**（一次都没触发 → 阈值太高或计数没接上，A 不过则 B 无意义）
- 每一行的 `区内` 值 **≥ 阈值**，违例行数必须为 **0**

**B（不变差）**：`群攻键` 绑定 / 留空 交替各跑 3 轮 × 10 分钟，记每轮每分钟经验增长。通过线：**绑定组中位数 ≥ 留空组中位数**（群攻是净增的输出路径，不该让经验下降；若下降，最可能是群攻耗蓝拖垮了续航，先看喝蓝频率再调 `群攻间隔`）。

**C（症状消失）**：用户主观确认被怪围住时确实放得出群攻，且不再出现「被围时左右扭」。

A/B/C/D 未全部通过前，不得声称功能已完成。

## 6. 明确不做

- **独立 `群攻区宽/高`** —— §3.2，等标定
- **MP 门控** —— 交给现成的 `喝蓝阈值`；空蓝的表现是技能放不出来，不值得为它加一层判定
- **定频模式的群攻** —— 没有找怪信息，无从计数
- **计数去抖** —— §3.10
- **「群攻优先于寻怪」的额外分支** —— §3.8，天然互斥
- **改 `decision_log_line` 格式** —— §4
- **动 `attack_turn_direction` / `seek_direction` / 转向冷却** —— 沿用 `2026-08-07` spec §8 的禁区，本次只在转向门上**加一个与判据**，不改选边规则本身
