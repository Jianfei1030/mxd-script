# ok-mxd MVP 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 基于 ok-ww 框架骨架，交付冒险岛怀旧服可用的站桩自动打怪 MVP(spec 的 M0 收尾 + M1 + M2)。

**Architecture:** 复制 ok-ww 仓库骨架保留 `ok/` 框架，新写 `config.py` + `src/`(detect 检测层 / task 任务层 / scene 状态缓存)。检测逻辑与决策逻辑分离为纯函数模块(`bars.py`、`farm_logic.py` 等），用存档帧 + 合成图做离线 unittest;PostMessage/WGC 等实弹验证集中在 `tests/test_m0_live.py` 作为 M0 卡点。

**Tech Stack:** Python 3.12（复用 `H:\ok-mxd\data\apps\ok-ww\python\python.exe`，依赖已装全）、OpenCV、pywin32、PySide6/qfluentwidgets（框架 GUI)、stdlib unittest（不装 pytest，不装任何新包）。

**范围说明：** 本计划覆盖 spec §8 的 M0/M1/M2/M3(YOLO 已按用户决策并入 MVP,Task 9-11)。M4(GUI 打磨/注册启动器）另行立计划。

**Spec:** `H:\ok-mxd\docs\superpowers\specs\2026-08-05-ok-mxd-design.md`

## Global Constraints

- Python 解释器固定为 `H:\ok-mxd\data\apps\ok-ww\python\python.exe`；计划中用 `$PY` 代指。**禁止往该嵌入式环境 pip install 任何包**；训练/标注等额外依赖一律装进独立 venv(Task 9/10 的 `H:\ok-mxd\yolo-venv`)。测试一律用 stdlib `unittest`。
- 项目根目录 `H:\ok-mxd\ok-mxd`；所有测试命令在该目录下运行（`python -m unittest` 依赖 cwd 在 sys.path)。
- `ok/` 框架目录**只读**，发现框架问题在 `src/` 侧绕过，不改框架。
- 游戏窗口：进程 `Maplestory_Classic.exe`，窗口类 `UnityWndClass`，标题 `冒险岛怀旧服`。
- 校准分辨率 2560x1440；所有 ROI 一律存帧比例坐标（分数），运行时再换算像素；**挂机只允许 2560x1440，主循环带帧尺寸守卫，不符直接停任务**(Task 2 Step 4.5 的分辨率实验仅记录结论，不改变此约束）。
- `config.py` 的 `'interaction'` 合法值只有 `'PostMessage'/'Genshin'/'ForegroundPostMessage'/'Pynput'/'PyDirect'`(DeviceManager.py:104-117)，其他值会把类赋成字符串、实例化时才炸。
- 运行约束（写进 README)：挂机期间不得拖动/缩放/折叠游戏内 UI 面板，不得改分辨率。
- 按键名以 `ok/device/interaction_methods/keys.py` 的 `vk_key_dict` 为准（已确认有效：`shift` `ctrl` `home` `insert` `delete` `pageup` `esc` `z`)。
- 2560x1440 实测校准值（来自存档帧，直接用）:
  - HP 条 x 943-1139, y 1406-1432;MP 条 x 1145-1342, y 1406-1432;EXP 条 x 1357-1571（满）, y 1406-1432;EXP 填充有 r 206→147 渐变，YELLOW 的 r 下限取 120（掩码 r≥170 会在渐变尾断裂）
  - 小地图内框 x 11-281, y 135-356
  - 快捷栏格子：左上角 (1746,1171)，格 ~67x65,4 列 2 行；行1 = shift/insert/home/pageup，行2 = ctrl/delete/end/pagedown
- commit message 结尾带 `Co-Authored-By: Claude <noreply@anthropic.com>`。

---

### Task 1: 项目脚手架

**Files:**
- Create: `H:\ok-mxd\ok-mxd\`（从 `H:\ok-mxd\data\apps\ok-ww\repo` 复制骨架）
- Create: `config.py`, `src/__init__.py`, `src/globals.py`, `src/Labels.py`, `src/scene/__init__.py`, `src/scene/MapleScene.py`, `src/task/__init__.py`, `src/task/BaseMapleTask.py`, `src/task/MapleFarmTask.py`, `src/detect/__init__.py`, `assets/coco_annotations.json`, `tests/__init__.py`, `.gitignore`
- Create: `screenshots/test_frames/training_ground_full_2560x1440.png`（由 `H:\ok-mxd\wgc_test.png` 复制）

**Interfaces:**
- Produces: 可启动的 ok-mxd 骨架；`config` dict(config.py）供全部后续任务；存档测试帧路径 `screenshots/test_frames/training_ground_full_2560x1440.png`。

- [ ] **Step 1: 复制骨架（排除鸣潮专属与运行产物）**

```bash
mkdir -p /h/ok-mxd/ok-mxd
cd /h/ok-mxd/data/apps/ok-ww/repo
# 排除鸣潮专属与运行产物;pyappify.yml/ok.yml 指向 ok-ww 的发布仓库(更新会拉 ok-ww 覆盖本项目),必须排除;
# i18n/ 是鸣潮任务串译文(我们的任务直接用中文串,tr() 会回退原文),一并排除
tar --exclude=.git --exclude=./src --exclude=./assets --exclude=./config.py \
    --exclude=./configs --exclude=./logs --exclude=./screenshots \
    --exclude=./pyappify.yml --exclude=./ok.yml --exclude=./i18n \
    --exclude=./README.md --exclude=./README_en.md --exclude=./readme \
    --exclude='__pycache__' --exclude='*.pyc' -cf - . | tar -xf - -C /h/ok-mxd/ok-mxd
```

- [ ] **Step 2: 写 config.py 与 src 桩文件**

`H:\ok-mxd\ok-mxd\config.py`:

```python
import os

from qfluentwidgets import FluentIcon

from ok import ConfigOption

version = "v0.1.0"

key_config_option = ConfigOption('游戏按键', {
    '攻击键': 'shift',
    '血药键': 'home',
    '蓝药键': 'insert',
    '回城卷键(可留空)': '',
    '拾取键': 'z',
}, description='冒险岛游戏内按键,与游戏内键盘设置保持一致', config_description={
    '回城卷键(可留空)': '低血保命用。留空则低血时只停止任务不逃跑',
}, show_at_tab=True, icon=FluentIcon.GAME)

config = {
    'debug': False,
    'use_gui': True,
    'config_folder': 'configs',
    'gui_icon': 'icons/icon.png',
    'global_configs': [key_config_option],
    'ocr': {
        'lib': 'onnxocr',
        'auto_simplify': True,
        'params': {'use_openvino': True, 'use_npu': True},
    },
    'my_app': ['src.globals', 'Globals'],
    'start_timeout': 60,
    'wait_until_settle_time': 0,
    'template_matching': {
        'coco_feature_json': os.path.join('assets', 'coco_annotations.json'),
        'default_horizontal_variance': 0.002,
        'default_vertical_variance': 0.002,
        'default_threshold': 0.8,
    },
    'windows': {
        'title': '冒险岛怀旧服',
        'exe': 'Maplestory_Classic.exe',
        'hwnd_class': 'UnityWndClass',
        'interaction': 'PostMessage',
        'capture_method': ['WGC', 'BitBlt_RenderFull'],
        'check_hdr': False,
        'force_no_hdr': False,
        'check_night_light': False,
        'force_no_night_light': False,
    },
    'window_size': {'width': 1200, 'height': 800, 'min_width': 1200, 'min_height': 800},
    # ratio 设 None 显式关闭比例检查(冒险岛有 4:3/窗口化,不照搬鸣潮 16:9)
    'supported_resolution': {'ratio': None, 'resize_to': [], 'min_size': (1024, 768)},
    'links': {'default': {}},
    'about': """
    <p style="color:red;"><strong>本软件仅供个人学习 Python、计算机视觉、UI 自动化使用。</strong></p>
    <p style="color:red;"><strong>使用本软件可能导致账号被封禁,后果自负。</strong></p>
""",
    'screenshots_folder': 'screenshots',
    'gui_title': 'OK-MXD',
    'log_file': 'logs/ok-mxd.log',
    'error_log_file': 'logs/ok-mxd_error.log',
    'version': version,
    'onetime_tasks': [],
    'trigger_tasks': [["src.task.MapleFarmTask", "MapleFarmTask"]],
    'scene': ["src.scene.MapleScene", "MapleScene"],
}
```

`src/__init__.py`、`src/scene/__init__.py`、`src/task/__init__.py`、`src/detect/__init__.py`、`tests/__init__.py`：空文件。

`src/globals.py`:

```python
from PySide6.QtCore import QObject


class Globals(QObject):
    """应用级单例(对应 config['my_app'])。YOLO 等重型资源后续按需加到这里。"""

    def __init__(self, exit_event):
        super().__init__()
        self.exit_event = exit_event
```

`src/Labels.py`:

```python
# 模板名常量。assets/coco_annotations.json 增加特征时在此同步。
```

`src/scene/MapleScene.py`:

```python
from ok import BaseScene


class MapleScene(BaseScene):
    """框架要求 scene 类存在(config['scene'])。状态缓存按需后续添加,MVP 为空壳。"""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
```

`src/task/BaseMapleTask.py`（桩，Task 5 充实）:

```python
from ok import BaseTask, Logger

logger = Logger.get_logger(__name__)


class BaseMapleTask(BaseTask):

    def stop_farming(self, reason):
        self.log_warning(f'停止打怪:{reason}', notify=True)
        self.disable()
```

`src/task/MapleFarmTask.py`（桩，Task 5 充实）:

```python
from qfluentwidgets import FluentIcon

from ok import TriggerTask
from src.task.BaseMapleTask import BaseMapleTask


class MapleFarmTask(TriggerTask, BaseMapleTask):

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self.name = "自动打怪"
        self.description = "站桩定频攻击+自动喝药+低血保命"
        self.icon = FluentIcon.GAME
        self.trigger_interval = 0.1
        self.default_config.update({'_enabled': False})

    def run(self):
        pass
```

`assets/coco_annotations.json`:

```json
{"images": [], "annotations": [], "categories": []}
```

`.gitignore`:

```
__pycache__/
*.pyc
logs/
configs/
screenshots/*
!screenshots/test_frames/
```

- [ ] **Step 3: 归档测试帧 + git init**

```bash
mkdir -p /h/ok-mxd/ok-mxd/screenshots/test_frames
cp /h/ok-mxd/wgc_test.png /h/ok-mxd/ok-mxd/screenshots/test_frames/training_ground_full_2560x1440.png
cd /h/ok-mxd/ok-mxd && git init
git config user.email >/dev/null 2>&1 || git config user.email "ok-mxd@local"
git config user.name  >/dev/null 2>&1 || git config user.name  "ok-mxd"
git add -A && git commit -m "chore: scaffold ok-mxd from ok-ww skeleton

Co-Authored-By: Claude <noreply@anthropic.com>"
```

- [ ] **Step 4: 验证 GUI 能启动且识别游戏窗口（游戏需在运行）**

```bash
cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" main_debug.py
```

Expected:GUI 出现；日志（终端或 `logs/ok-mxd.log`）出现 `hwnd changed from 0 to <数字> ... Maplestory_Classic.exe UnityWndClass`;Start 页显示游戏窗口 2560x1440。确认后关闭 GUI。
若 FeatureSet 报 COCO 解析错：检查 `assets/coco_annotations.json` 是否为 Step 2 的内容且 UTF-8 无 BOM。

---

### Task 2: M0 实弹验证（WGC/PostMessage/血条/OCR 卡点）

**Files:**
- Create: `scripts/capture_frame.py`
- Test: `tests/test_m0_live.py`

**Interfaces:**
- Consumes: Task 1 骨架、`ok` 框架。
- Produces: `scripts/capture_frame.build_capture() -> (exit_event, hwnd_window, capture_method)`（后续所有 live 测试与素材采集复用）;M0 通过/降级结论。

**卡点决策规则（spec §6):** `test_postmessage_response` 失败 = PostMessage 被 BlackCipher 拦截 → 两级降级（config.py `'interaction'` 的**合法值只有** `'PostMessage'/'Genshin'/'ForegroundPostMessage'/'Pynput'/'PyDirect'`,DeviceManager.py:104-117，写错值会把类赋成字符串、实例化时才炸且报错指不到根因）:
1. 先改 `'ForegroundPostMessage'` 重跑本测试（游戏窗口置前台）——仍是 PostMessage 家族，最接近原方案
2. 仍失败再改 `'PyDirect'` 重跑——SendInput 驱动级，兼容性最好，但抢键鼠、产品定位降级为前台挂机，向用户明示；此后 Task 5 的走位拾取保持关闭
任一级 PASS 即继续，并把最终档位告知用户。

- [ ] **Step 1: 写共享截屏助手 `scripts/capture_frame.py`**

```python
"""抓取一帧游戏画面。用法: python scripts/capture_frame.py <输出路径.png>
(直接运行该脚本时 sys.path[0] 是 scripts/,先把项目根插进去才能 import ok)"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import threading
import time


class _Opt(dict):
    validator = None


class _FakeGlobalConfig:
    def get_config(self, option):
        return _Opt()


class _FakeDeviceManager:
    config = {'selected_hwnd': 0}
    capture_method = None


def build_capture():
    """返回 (exit_event, hwnd_window, capture_method)。用毕需 exit_event.set()。"""
    from ok.device.capture_methods.hwnd_window import HwndWindow
    from ok.device.capture_methods.windows_graphics import WindowsGraphicsCaptureMethod

    exit_event = threading.Event()
    win = HwndWindow(exit_event, title='冒险岛怀旧服', exe_name='Maplestory_Classic.exe',
                     hwnd_class='UnityWndClass',
                     global_config=_FakeGlobalConfig(), device_manager=_FakeDeviceManager())
    deadline = time.time() + 10
    while not win.exists and time.time() < deadline:
        time.sleep(0.5)
    if not win.exists:
        raise RuntimeError('未找到游戏窗口(冒险岛怀旧服/UnityWndClass)')
    cap = WindowsGraphicsCaptureMethod(win)
    time.sleep(2)
    return exit_event, win, cap


if __name__ == '__main__':
    import cv2

    out = sys.argv[1] if len(sys.argv) > 1 else 'screenshots/frame.png'
    ev, win, cap = build_capture()
    frame = cap.get_frame()
    assert frame is not None, 'WGC 取帧失败'
    cv2.imwrite(out, frame)
    print(f'saved {out} {frame.shape}')
    ev.set()
```

- [ ] **Step 2: 写失败的 live 测试 `tests/test_m0_live.py`**

```python
import time
import unittest

import numpy as np


def game_running():
    import win32gui
    found = []

    def cb(h, _):
        if (win32gui.IsWindowVisible(h)
                and win32gui.GetClassName(h) == 'UnityWndClass'
                and win32gui.GetWindowText(h) == '冒险岛怀旧服'):
            found.append(h)

    win32gui.EnumWindows(cb, None)
    return bool(found)


@unittest.skipUnless(game_running(), '游戏未运行,跳过 M0 实弹测试')
class TestM0Live(unittest.TestCase):

    @classmethod
    def setUpClass(cls):
        from scripts.capture_frame import build_capture
        cls.exit_event, cls.win, cls.cap = build_capture()

    @classmethod
    def tearDownClass(cls):
        try:  # 确保不留打开的面板(某轮按键没生效时兜底还原)
            from ok.device.interaction_methods.post_message import PostMessageInteraction
            PostMessageInteraction(cls.cap, cls.win).send_key('esc', 0.05)
        except Exception:
            pass
        cls.exit_event.set()

    def _frame(self):
        frame = self.cap.get_frame()
        self.assertIsNotNone(frame, 'WGC 取帧失败')
        return frame

    @staticmethod
    def _diff(a, b):
        return float(np.abs(a.astype(np.int32) - b.astype(np.int32)).mean())

    def test_wgc_frame_clear(self):
        frame = self._frame()
        self.assertGreaterEqual(frame.shape[0], 768, '帧高度异常')
        self.assertGreater(float(frame.mean()), 5.0, '帧为黑图')

    def test_bar_reading_live(self):
        from src.detect import bars
        frame = self._frame()
        for name, val in [('hp', bars.read_hp(frame)), ('mp', bars.read_mp(frame)),
                          ('exp', bars.read_exp(frame))]:
            self.assertGreaterEqual(val, 0.0, name)
            self.assertLessEqual(val, 1.0, name)

    def test_quickslot_count_ocr_live(self):
        from src.detect import potions
        frame = self._frame()
        # 只验证能读出非负整数,不锁定具体数值(药水会被消耗)
        blue = potions.read_slot_count(frame, 'insert')
        red = potions.read_slot_count(frame, 'home')
        self.assertIsNotNone(blue, '蓝药数量 OCR 失败')
        self.assertIsNotNone(red, '红药数量 OCR 失败')

    def test_postmessage_response(self):
        """判据:面板开合引起**中央 ROI** 帧差显著高于同区域环境基线(3 倍且 >5.0)。
        依次尝试 'i'(道具栏)与 'esc'(系统菜单),任一响应即通过;每个键发两次(开+关还原)。
        全帧比对不可用:面板在 2560x1440 上占比小,训练场 ambient 会淹没信号(假阴性)。
        建议在空频道/安静地图跑。失败先把差异帧存 screenshots/ 供人眼复核,再下降级结论。"""
        import cv2
        from ok.device.interaction_methods.post_message import PostMessageInteraction
        inter = PostMessageInteraction(self.cap, self.win)

        def roi(f):
            h, w = f.shape[:2]
            return f[int(0.2 * h):int(0.8 * h), int(0.25 * w):int(0.75 * w)]

        responded = False
        f2 = f3 = None
        for key in ('i', 'esc'):
            f1 = self._frame()
            time.sleep(0.8)
            f2 = self._frame()
            ambient = self._diff(roi(f1), roi(f2))   # 同区域环境动画基线
            inter.send_key(key, 0.05)
            time.sleep(0.8)
            f3 = self._frame()
            after = self._diff(roi(f2), roi(f3))
            inter.send_key(key, 0.05)                # 关闭面板,还原状态
            time.sleep(0.5)
            if after > max(3.0 * ambient, 5.0):
                responded = True
                break
        if not responded and f2 is not None:
            cv2.imwrite('screenshots/m0_fail_before.png', f2)
            cv2.imwrite('screenshots/m0_fail_after.png', f3)
        self.assertTrue(responded,
                        'i/esc 中央 ROI 均无响应,PostMessage 疑似被拦截;'
                        '差异帧已存 screenshots/m0_fail_*.png,人眼复核后再定降级')
```

注：`test_bar_reading_live` 与 `test_quickslot_count_ocr_live` 依赖 Task 3/6 的模块；本任务先只跑前两个测试，后两个在对应任务完成后补跑通过（见 Task 3/6 收尾步骤）。

- [ ] **Step 3: 运行 WGC 测试确认通过**

Run: `cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_m0_live.TestM0Live.test_wgc_frame_clear -v`
Expected: PASS（其余测试此时 ERROR/FAIL 属正常，依赖尚未实现）

- [ ] **Step 4: 运行 PostMessage 卡点测试**

Run: `cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_m0_live.TestM0Live.test_postmessage_response -v`
Expected: PASS = 后台方案成立，继续；
FAIL = 先人眼复核 `screenshots/m0_fail_*.png`（防假阴性），确认无响应后按本任务开头"卡点决策规则"两级降级，任一级 PASS 后继续，并把最终档位告知用户。

- [ ] **Step 4.5: 分辨率缩放验证（spec M0-5，手动，不依赖任何 Task 3+ 模块）**

1. 当前 2560x1440 下跑 `scripts/capture_frame.py screenshots/res_2560.png`
2. 游戏设置里改一个别的分辨率（如 1920x1080 窗口化），再截 `screenshots/res_other.png`
3. **手工量**两张图的 HP 条像素宽度（画图/Photoshop 取色即可，2560x1440 下应为 196px)
4. 结论（固定像素 or 随分辨率缩放）记进 README;**无论结论如何，运行约束固定为"只在 2560x1440 挂机"**,Task 5 的帧尺寸守卫强制（连续 10 帧不符才停，防窗口切换瞬间误停）——比例 ROI 在该约束下恒成立
5. 改回 2560x1440

- [ ] **Step 5: Commit**

```bash
cd /h/ok-mxd/ok-mxd && git add scripts tests && git commit -m "test: M0 live verification (WGC/PostMessage/bars/quickslot OCR)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 血蓝经验条检测 bars.py

**Files:**
- Create: `src/detect/bars.py`
- Test: `tests/test_bars.py`

**Interfaces:**
- Consumes: 存档帧 `screenshots/test_frames/training_ground_full_2560x1440.png`(HP/MP 满，EXP=70.58%)。
- Produces: `bars.read_hp(frame)->float`、`bars.read_mp(frame)->float`、`bars.read_exp(frame)->float`、`bars.read_bar_percent(frame, roi_fractions, color_range)->float`（均为 0..1，按列填充率计算）;`bars.HP_BAR/MP_BAR/EXP_BAR`、`bars.RED/BLUE/YELLOW` 常量。

- [ ] **Step 1: 写失败的测试 `tests/test_bars.py`**

```python
import unittest

import cv2
import numpy as np

from src.detect import bars

FRAME = 'screenshots/test_frames/training_ground_full_2560x1440.png'


def make_bar(fill, w=200, h=26, color=(0, 0, 220)):
    img = np.full((h, w, 3), (20, 20, 20), np.uint8)
    if fill > 0:
        img[:, :max(1, int(w * fill))] = color
    return img


class TestBarsSynthetic(unittest.TestCase):
    FULL = (0.0, 0.0, 1.0, 1.0)

    def test_full(self):
        self.assertAlmostEqual(bars.read_bar_percent(make_bar(1.0), self.FULL, bars.RED), 1.0, delta=0.03)

    def test_half(self):
        self.assertAlmostEqual(bars.read_bar_percent(make_bar(0.5), self.FULL, bars.RED), 0.5, delta=0.03)

    def test_empty(self):
        self.assertAlmostEqual(bars.read_bar_percent(make_bar(0.0), self.FULL, bars.RED), 0.0, delta=0.03)


class TestBarsRealFrame(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        cls.frame = cv2.imread(FRAME)
        assert cls.frame is not None, f'存档帧缺失: {FRAME}'

    def test_hp_full(self):
        self.assertGreaterEqual(bars.read_hp(self.frame), 0.95)

    def test_mp_full(self):
        self.assertGreaterEqual(bars.read_mp(self.frame), 0.95)

    def test_exp(self):
        self.assertAlmostEqual(bars.read_exp(self.frame), 0.706, delta=0.05)


if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_bars -v`
Expected: FAIL(`ModuleNotFoundError: src.detect.bars`)

- [ ] **Step 3: 实现 `src/detect/bars.py`**

```python
"""HP/MP/EXP 条读取。ROI 为帧比例坐标,2560x1440 校准(见计划 Global Constraints)。"""
import cv2

HP_BAR = (943 / 2560, 1406 / 1440, 1139 / 2560, 1432 / 1440)
MP_BAR = (1145 / 2560, 1406 / 1440, 1342 / 2560, 1432 / 1440)
EXP_BAR = (1357 / 2560, 1406 / 1440, 1571 / 2560, 1432 / 1440)

RED = {'b': (0, 90), 'g': (0, 90), 'r': (150, 255)}
BLUE = {'b': (150, 255), 'g': (80, 255), 'r': (0, 100)}
# EXP 填充色有 r 206→147 的色相渐变,r 下限必须放到 120,否则掩码在渐变尾部断裂;
# 灰槽 (177,177,177) 被 b≤110 挡住不会误计(2026-08-05 像素级实测)
YELLOW = {'b': (0, 110), 'g': (170, 255), 'r': (120, 255)}


def _roi_pixels(frame, fractions):
    h, w = frame.shape[:2]
    x1, y1, x2, y2 = fractions
    return int(x1 * w), int(y1 * h), int(x2 * w), int(y2 * h)


def read_bar_percent(frame, roi_fractions, color_range):
    """按列填充率:一列中 >=50% 像素落在颜色区间内记为已填充。"""
    x1, y1, x2, y2 = _roi_pixels(frame, roi_fractions)
    roi = frame[y1:y2, x1:x2]
    if roi.size == 0:
        return 0.0
    lower = (color_range['b'][0], color_range['g'][0], color_range['r'][0])
    upper = (color_range['b'][1], color_range['g'][1], color_range['r'][1])
    mask = cv2.inRange(roi, lower, upper)
    filled_cols = ((mask > 0).sum(axis=0) >= roi.shape[0] * 0.5).sum()
    return float(filled_cols) / mask.shape[1]


def read_hp(frame):
    return read_bar_percent(frame, HP_BAR, RED)


def read_mp(frame):
    return read_bar_percent(frame, MP_BAR, BLUE)


def read_exp(frame):
    return read_bar_percent(frame, EXP_BAR, YELLOW)
```

- [ ] **Step 4: 运行确认通过**

Run: `cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_bars -v`
Expected: 6 项 PASS。若 `test_exp` 偏差大：EXP 条满刻度终点 1552 是估算值，用 `python -c` 打印 `bars.read_exp(frame)` 微调 EXP_BAR 右边界使读数 ≈0.706。

- [ ] **Step 5: 补跑 M0 血条 live 测试并提交**

Run: `cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_m0_live.TestM0Live.test_bar_reading_live -v`
Expected: PASS（游戏需在运行）

```bash
cd /h/ok-mxd/ok-mxd && git add src/detect tests/test_bars.py && git commit -m "feat: HP/MP/EXP bar reading via column fill ratio

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 决策纯逻辑 farm_logic.py

**Files:**
- Create: `src/task/farm_logic.py`
- Test: `tests/test_farm_logic.py`

**Interfaces:**
- Produces（全部为纯函数，Task 5/6/8 消费）:
  - `need_hp_potion(hp: float, threshold: float) -> bool`
  - `need_mp_potion(mp: float, threshold: float) -> bool`
  - `is_emergency(hp: float, threshold: float) -> bool`
  - `emergency_action(return_scroll_key: str) -> str`（`'return_scroll'` | `'stop'`)
  - `potion_not_working(streak: int, limit: int) -> bool`
  - `potions_exhausted(hp, hp_threshold, hp_count, mp, mp_threshold, mp_count) -> str | None`（`'hp'`/`'mp'`/`None`;count 为 `None` 表示未知，不判耗尽）
  - `is_dead(hp: float, threshold: float = 0.02) -> bool`（空血判定，Task 5 连续 N 帧确认死亡用）
  - `should_attack(now: float, last: float, interval: float) -> bool`
  - `should_pickup(now: float, last: float, interval: float, enabled: bool) -> bool`
  - `parse_buff_config(text: str) -> list[tuple[str, str]]`（如 `'magic_shield=q,armor=w'` → `[('magic_shield','q'),('armor','w')]`)

- [ ] **Step 1: 写失败的测试 `tests/test_farm_logic.py`**

```python
import unittest

from src.task import farm_logic as fl


class TestFarmLogic(unittest.TestCase):

    def test_need_hp_potion(self):
        self.assertTrue(fl.need_hp_potion(0.5, 0.7))
        self.assertFalse(fl.need_hp_potion(0.7, 0.7))

    def test_need_mp_potion(self):
        self.assertTrue(fl.need_mp_potion(0.2, 0.35))
        self.assertFalse(fl.need_mp_potion(0.9, 0.35))

    def test_emergency(self):
        self.assertTrue(fl.is_emergency(0.24, 0.25))
        self.assertFalse(fl.is_emergency(0.25, 0.25))

    def test_emergency_action(self):
        self.assertEqual(fl.emergency_action('t'), 'return_scroll')
        self.assertEqual(fl.emergency_action(''), 'stop')
        self.assertEqual(fl.emergency_action('  '), 'stop')

    def test_potion_not_working(self):
        self.assertTrue(fl.potion_not_working(5, 5))
        self.assertFalse(fl.potion_not_working(4, 5))

    def test_potions_exhausted(self):
        self.assertEqual(fl.potions_exhausted(0.5, 0.7, 0, 0.9, 0.35, 10), 'hp')
        self.assertEqual(fl.potions_exhausted(0.9, 0.7, 10, 0.2, 0.35, 0), 'mp')
        self.assertIsNone(fl.potions_exhausted(0.9, 0.7, 10, 0.9, 0.35, 10))
        self.assertIsNone(fl.potions_exhausted(0.5, 0.7, None, 0.9, 0.35, None))  # 未知不判

    def test_should_attack(self):
        self.assertTrue(fl.should_attack(10.0, 8.4, 1.5))
        self.assertFalse(fl.should_attack(10.0, 9.0, 1.5))

    def test_should_pickup(self):
        self.assertFalse(fl.should_pickup(100.0, 0.0, 30.0, False))
        self.assertTrue(fl.should_pickup(100.0, 0.0, 30.0, True))
        self.assertFalse(fl.should_pickup(20.0, 0.0, 30.0, True))

    def test_parse_buff_config(self):
        self.assertEqual(fl.parse_buff_config('magic_shield=q,armor=w'),
                         [('magic_shield', 'q'), ('armor', 'w')])
        self.assertEqual(fl.parse_buff_config(''), [])
        self.assertEqual(fl.parse_buff_config('  '), [])
        self.assertEqual(fl.parse_buff_config('bad-entry,good=x'), [('good', 'x')])

    def test_is_dead(self):
        self.assertTrue(fl.is_dead(0.0))
        self.assertTrue(fl.is_dead(0.01))
        self.assertFalse(fl.is_dead(0.02))
        self.assertFalse(fl.is_dead(0.5))


if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_farm_logic -v`
Expected: FAIL(`ModuleNotFoundError: src.task.farm_logic`)

- [ ] **Step 3: 实现 `src/task/farm_logic.py`**

```python
"""打怪决策纯逻辑。不依赖游戏/框架,全部可离线单测。"""


def need_hp_potion(hp_percent, threshold):
    return 0 <= hp_percent < threshold


def need_mp_potion(mp_percent, threshold):
    return 0 <= mp_percent < threshold


def is_emergency(hp_percent, threshold):
    return 0 <= hp_percent < threshold


def emergency_action(return_scroll_key):
    if return_scroll_key and return_scroll_key.strip():
        return 'return_scroll'
    return 'stop'


def potion_not_working(streak, limit):
    return streak >= limit


def potions_exhausted(hp, hp_threshold, hp_count, mp, mp_threshold, mp_count):
    """count 为 None 表示 OCR 未读出(未知),不判耗尽。"""
    if hp_count == 0 and need_hp_potion(hp, hp_threshold):
        return 'hp'
    if mp_count == 0 and need_mp_potion(mp, mp_threshold):
        return 'mp'
    return None


def should_attack(now, last_attack_time, interval):
    return now - last_attack_time >= interval


def should_pickup(now, last_pickup_time, interval, enabled):
    return enabled and now - last_pickup_time >= interval


def parse_buff_config(text):
    """'magic_shield=q,armor=w' -> [('magic_shield','q'),('armor','w')]"""
    result = []
    for entry in (text or '').split(','):
        entry = entry.strip()
        if '=' in entry:
            name, key = entry.split('=', 1)
            if name.strip() and key.strip():
                result.append((name.strip(), key.strip()))
    return result


def is_dead(hp_percent, threshold=0.02):
    return hp_percent <= threshold
```

- [ ] **Step 4: 运行确认通过**

Run: `cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_farm_logic -v`
Expected: 10 项 PASS

- [ ] **Step 5: Commit**

```bash
cd /h/ok-mxd/ok-mxd && git add src/task/farm_logic.py tests/test_farm_logic.py && git commit -m "feat: pure farm decision logic

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 打怪主循环 MapleFarmTask(M1 核心）

**Files:**
- Modify: `src/task/BaseMapleTask.py`（增加 read_hp/read_mp/read_exp)
- Modify: `src/task/MapleFarmTask.py`（完整实现，替换 Task 1 的桩）
- Test: `tests/test_farm_task_offline.py`

**Interfaces:**
- Consumes: `bars.read_hp/read_mp`、`farm_logic.*`、全局配置 `'游戏按键'`(config.py)。
- Produces: `BaseMapleTask.read_hp(frame=None)->float`、`read_mp`、`read_exp`、`stop_farming(reason)`;`MapleFarmTask` 完整任务（GUI 可启用）。

- [ ] **Step 1: 写离线测试（用存档帧驱动 run() 不发真按键）**

`tests/test_farm_task_offline.py`:

```python
import unittest
from unittest.mock import MagicMock, call, patch

import cv2

from src.task.MapleFarmTask import DEFAULT_CONFIG, MapleFarmTask

FRAME = 'screenshots/test_frames/training_ground_full_2560x1440.png'
KEYS = {'攻击键': 'shift', '血药键': 'home', '蓝药键': 'insert',
        '回城卷键(可留空)': '', '拾取键': 'z'}


def make_task(**cfg_overrides):
    """config 直接取自模块级 DEFAULT_CONFIG,与 __init__ 同源,
    后续任务新增配置键/状态时本测试不再需要手工同步。"""
    task = MapleFarmTask.__new__(MapleFarmTask)  # 绕过框架 __init__
    task.config = {**DEFAULT_CONFIG, 'Buff键位': '', **cfg_overrides}
    task._reset_state()
    task.send_key = MagicMock()
    task.stop_farming = MagicMock()
    task.get_global_config = MagicMock(return_value=dict(KEYS))
    return task


def run_with_frame(task, hp=None):
    """以存档帧驱动一次 run();hp 不为 None 时替换血条读数。"""
    frame_p = patch.object(MapleFarmTask, 'frame',
                           new=property(lambda self: cv2.imread(FRAME)))
    patches = [frame_p, patch('time.time', return_value=100.0)]
    if hp is not None:
        patches.append(patch('src.task.MapleFarmTask.bars.read_hp', return_value=hp))
    for p in patches:
        p.start()
    try:
        task.run()
    finally:
        for p in patches:
            p.stop()


class TestFarmTaskOffline(unittest.TestCase):

    def test_full_hp_attacks_only(self):
        task = make_task()
        run_with_frame(task)
        self.assertIn(call('shift'), task.send_key.call_args_list)
        task.stop_farming.assert_not_called()

    def test_low_hp_no_scroll_potions_then_stops(self):
        task = make_task()
        run_with_frame(task, hp=0.2)
        self.assertIn(call('home'), task.send_key.call_args_list)  # 先喝血
        task.stop_farming.assert_called_once()                      # 再停(未配回城卷)

    def test_low_hp_with_scroll_potions_scrolls_stops(self):
        task = make_task()
        task.get_global_config = MagicMock(return_value={**KEYS, '回城卷键(可留空)': 't'})
        run_with_frame(task, hp=0.2)
        calls = task.send_key.call_args_list
        self.assertIn(call('home'), calls)                          # 先喝血
        self.assertIn(call('t', after_sleep=2), calls)              # 再回城
        self.assertLess(calls.index(call('home')), calls.index(call('t', after_sleep=2)))
        task.stop_farming.assert_called_once()

    def test_dead_three_frames_stops(self):
        task = make_task()
        for _ in range(3):
            run_with_frame(task, hp=0.0)
        task.stop_farming.assert_called_once()

    def test_dead_counter_resets_on_recovery(self):
        task = make_task()
        run_with_frame(task, hp=0.0)
        run_with_frame(task, hp=0.0)
        run_with_frame(task, hp=0.9)   # 回血,计数清零
        run_with_frame(task, hp=0.0)
        task.stop_farming.assert_not_called()


if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_farm_task_offline -v`
Expected: FAIL(MapleFarmTask 还是桩）

- [ ] **Step 3: 充实 `src/task/BaseMapleTask.py`**

```python
from ok import BaseTask, Logger
from src.detect import bars

logger = Logger.get_logger(__name__)


class BaseMapleTask(BaseTask):

    def read_hp(self, frame=None):
        return bars.read_hp(frame if frame is not None else self.frame)

    def read_mp(self, frame=None):
        return bars.read_mp(frame if frame is not None else self.frame)

    def read_exp(self, frame=None):
        return bars.read_exp(frame if frame is not None else self.frame)

    def stop_farming(self, reason):
        self.log_warning(f'停止打怪:{reason}', notify=True)
        self.disable()
```

- [ ] **Step 4: 实现 `src/task/MapleFarmTask.py`（全量替换桩）**

```python
import time

from qfluentwidgets import FluentIcon

from ok import Logger, TriggerTask
from src.detect import bars
from src.task import farm_logic
from src.task.BaseMapleTask import BaseMapleTask

logger = Logger.get_logger(__name__)

# 模块级默认配置:__init__ 与离线测试共用同一份,新增键只改这里
DEFAULT_CONFIG = {
    '攻击间隔(秒)': 1.5,
    '喝血阈值': 0.7,
    '喝蓝阈值': 0.35,
    '保命血线': 0.25,
    '死亡判定线': 0.02,
    '死亡确认帧数': 3,
    '喝药无效上限': 5,
    '拾取开关': False,
    '拾取间隔(秒)': 30,
}

CALIBRATED_SIZE = (2560, 1440)  # 只在此分辨率挂机(README 约束)


class MapleFarmTask(TriggerTask, BaseMapleTask):

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self.name = "自动打怪"
        self.description = "站桩定频攻击+自动喝药+低血保命"
        self.icon = FluentIcon.GAME
        self.trigger_interval = 0.1  # ~10Hz 轮询,保命响应足够快
        self.default_config.update(DEFAULT_CONFIG)
        self._reset_state()

    def _reset_state(self):
        """全部可变运行时状态。__init__ 与测试共用,新增状态只改这里。"""
        self._last_attack = 0.0
        self._last_pickup = 0.0
        self._hp_streak = 0
        self._last_hp = 1.0
        self._dead_frames = 0
        self._bad_size_frames = 0

    def run(self):
        # TaskExecutor 调度 trigger task 前已取帧(TaskExecutor.py:555),不要再 next_frame()
        frame = self.frame
        if frame is None:
            return
        h, w = frame.shape[:2]
        if (w, h) != CALIBRATED_SIZE:
            # 窗口切换/最小化瞬间会拿到异常尺寸帧,连续 10 帧确认再停
            self._bad_size_frames += 1
            if self._bad_size_frames >= 10:
                self.stop_farming(f'分辨率 {w}x{h} 非校准值 {CALIBRATED_SIZE[0]}x{CALIBRATED_SIZE[1]},请调回后再挂机')
            return
        self._bad_size_frames = 0

        now = time.time()
        keys = self.get_global_config('游戏按键')
        cfg = self.config

        hp = bars.read_hp(frame)
        mp = bars.read_mp(frame)

        # 0. 死亡判定(连续 N 帧空血;死亡弹窗后背景动画仍在,静止守卫兜不住,必须专判)
        #    确认窗口内无条件 return:血已空,喝药/回城都无意义;
        #    也防止血条被弹窗遮挡的单帧误读立刻烧掉一张回城卷
        if farm_logic.is_dead(hp, cfg['死亡判定线']):
            self._dead_frames += 1
            if self._dead_frames >= cfg['死亡确认帧数']:
                self.stop_farming('角色死亡')
            return
        self._dead_frames = 0

        # 1. 保命:先喝血 → 再回城 → 再停(尽力而为,不保证存活)
        if farm_logic.is_emergency(hp, cfg['保命血线']):
            self.send_key(keys['血药键'])
            scroll = keys.get('回城卷键(可留空)', '')
            if farm_logic.emergency_action(scroll) == 'return_scroll':
                self.log_warning(f'HP {hp:.0%} 触保命血线,使用回城卷', notify=True)
                self.send_key(scroll, after_sleep=2)
            else:
                self.log_warning(f'HP {hp:.0%} 触保命血线,未配置回城卷', notify=True)
            self.stop_farming('低血保命')
            return

        # 2. 喝血(连续无效检测:喝完 HP 不涨则累计,超上限停任务)
        if farm_logic.need_hp_potion(hp, cfg['喝血阈值']):
            self.send_key(keys['血药键'])
            self._hp_streak = self._hp_streak + 1 if hp <= self._last_hp + 0.01 else 0
            self._last_hp = hp
            if farm_logic.potion_not_working(self._hp_streak, cfg['喝药无效上限']):
                self.stop_farming('连续喝药无效')
                return
        else:
            self._hp_streak = 0
            self._last_hp = hp

        # 3. 喝蓝
        if farm_logic.need_mp_potion(mp, cfg['喝蓝阈值']):
            self.send_key(keys['蓝药键'])

        # 4. 定频攻击
        if farm_logic.should_attack(now, self._last_attack, cfg['攻击间隔(秒)']):
            self.send_key(keys['攻击键'])
            self._last_attack = now

        # 5. 拾取(默认关闭,靠宠物)
        if farm_logic.should_pickup(now, self._last_pickup, cfg['拾取间隔(秒)'], cfg['拾取开关']):
            self.send_key(keys['拾取键'])
            self._last_pickup = now
```

- [ ] **Step 5: 运行离线测试确认通过**

Run: `cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_farm_task_offline -v`
Expected: 5 项 PASS

- [ ] **Step 6: 实弹冒烟（游戏需在运行，站在有怪的安全点）**

启动 `main_debug.py` → Start 页启动 → 触发任务页启用"自动打怪" → 观察 1 分钟：角色按节拍放技能、HP/MP 低时喝药、F9 可急停。
通过判据：游戏内角色确实在攻击（血条扣怪/伤害数字出现），日志无异常。
注：若 Task 2 降级为 ForegroundPostMessage 或 PyDirect，游戏窗口必须保持前台。

- [ ] **Step 7: Commit**

```bash
cd /h/ok-mxd/ok-mxd && git add src tests && git commit -m "feat: farm main loop (cadence attack + potions + emergency)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: 药水数量 OCR 与耗尽保护 potions.py

**Files:**
- Create: `src/detect/potions.py`
- Modify: `src/task/MapleFarmTask.py`(run 内接入耗尽检查）
- Test: `tests/test_potions.py`

**Interfaces:**
- Consumes: 快捷栏校准值（Global Constraints)。
- Produces: `potions.parse_count(text)->int|None`、`potions.slot_roi(frame, slot_key)->tuple[int,int,int,int]`、`potions.read_slot_count(frame, slot_key, ocr_fn=None)->int|None`(`ocr_fn` 注入便于测试；为 None 时用 onnxocr 直读）、`potions.SLOT_KEYS`。

- [ ] **Step 1: 写失败的测试 `tests/test_potions.py`**

```python
import unittest

import cv2

from src.detect import potions

FRAME = 'screenshots/test_frames/training_ground_full_2560x1440.png'


class TestParseCount(unittest.TestCase):

    def test_digits(self):
        self.assertEqual(potions.parse_count('276'), 276)
        self.assertEqual(potions.parse_count(' 15 '), 15)

    def test_junk(self):
        self.assertIsNone(potions.parse_count(''))
        self.assertIsNone(potions.parse_count(None))
        self.assertIsNone(potions.parse_count('abc'))

    def test_mixed_takes_first_int(self):
        self.assertEqual(potions.parse_count('x15'), 15)


class TestSlotRoi(unittest.TestCase):

    def test_insert_right_of_shift(self):
        frame = cv2.imread(FRAME)
        xs, ys, ws, hs = potions.slot_roi(frame, 'shift')
        xi, yi, wi, hi = potions.slot_roi(frame, 'insert')
        self.assertGreater(xi, xs)
        self.assertEqual(yi, ys)

    def test_ctrl_below_shift(self):
        frame = cv2.imread(FRAME)
        xs, ys, _, _ = potions.slot_roi(frame, 'shift')
        xc, yc, _, _ = potions.slot_roi(frame, 'ctrl')
        self.assertEqual(xc, xs)
        self.assertGreater(yc, ys)

    def test_unknown_slot_returns_none(self):
        """用户把药水配到非快捷栏键(如 f1)时不许崩,返回 None(=未知,不判耗尽)。"""
        frame = cv2.imread(FRAME)
        self.assertIsNone(potions.slot_roi(frame, 'f1'))
        self.assertIsNone(potions.read_slot_count(frame, 'f1'))


class TestReadSlotCountRealFrame(unittest.TestCase):

    def test_blue_potion_276(self):
        """存档帧 insert 格蓝药数量为 276(离线 OCR 直读)。"""
        frame = cv2.imread(FRAME)
        self.assertEqual(potions.read_slot_count(frame, 'insert'), 276)

    def test_red_potion_15(self):
        frame = cv2.imread(FRAME)
        self.assertEqual(potions.read_slot_count(frame, 'home'), 15)


if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_potions -v`
Expected: FAIL(`ModuleNotFoundError: src.detect.potions`)

- [ ] **Step 3: 实现 `src/detect/potions.py`**

```python
"""快捷栏药水数量读取。格位为帧比例坐标,2560x1440 校准。"""
import re

import cv2

SLOT_ORIGIN = (1746 / 2560, 1171 / 1440)
SLOT_SIZE = (67 / 2560, 65 / 1440)
SLOT_KEYS = [
    ['shift', 'insert', 'home', 'pageup'],
    ['ctrl', 'delete', 'end', 'pagedown'],
]

_ocr_instance = None


def _get_ocr():
    """惰性创建 onnxocr 实例(离线直读,不走框架 executor)。"""
    global _ocr_instance
    if _ocr_instance is None:
        from onnxocr.onnx_paddleocr import ONNXPaddleOcr
        _ocr_instance = ONNXPaddleOcr(use_angle_cls=False, use_det=True, use_rec=True)
    return _ocr_instance


def parse_count(text):
    if not text:
        return None
    m = re.search(r'(\d{1,4})', text)
    return int(m.group(1)) if m else None


def slot_roi(frame, slot_key):
    """返回格子下半部(数字区域)的像素 (x, y, w, h)。未知键位返回 None。"""
    h, w = frame.shape[:2]
    col = row = None
    for r, row_keys in enumerate(SLOT_KEYS):
        if slot_key in row_keys:
            row, col = r, row_keys.index(slot_key)
            break
    if row is None:
        return None  # 非快捷栏键:读不了数量,交给"喝药无效"兜底,不许 raise 崩任务
    x = SLOT_ORIGIN[0] + col * SLOT_SIZE[0]
    y = SLOT_ORIGIN[1] + row * SLOT_SIZE[1]
    return (int(x * w), int((y + SLOT_SIZE[1] * 0.55) * h),
            int(SLOT_SIZE[0] * w), int(SLOT_SIZE[1] * 0.45 * h))


def read_slot_count(frame, slot_key, ocr_fn=None):
    """读格子里的数量。返回 int 或 None(读不出/空槽/未知键)。"""
    roi = slot_roi(frame, slot_key)
    if roi is None:
        return None
    x, y, w, h = roi
    crop = frame[y:y + h, x:x + w]
    if crop.size == 0:
        return None
    # 67x29 小号描边数字,PaddleOCR det 在小图上易漏检,放大 3 倍再送
    crop = cv2.resize(crop, None, fx=3, fy=3, interpolation=cv2.INTER_CUBIC)
    if ocr_fn is None:
        ocr_fn = lambda img: _get_ocr().ocr(img)
    texts = []
    for line in ocr_fn(crop) or []:
        try:
            texts.append(line[1][0] if isinstance(line[1], (tuple, list)) else str(line[1]))
        except (TypeError, IndexError):
            continue
    for t in texts:
        count = parse_count(t)
        if count is not None:
            return count
    return None


def prewarm():
    """任务启用时预热 OCR 模型(加载秒级),避免首次调用卡 10Hz 触发循环。"""
    _get_ocr()
```

- [ ] **Step 4: 运行确认通过**

Run: `cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_potions -v`
Expected: 7 项 PASS。若 OCR 读数不对：用 `scripts/capture_frame.py` 截帧 → 打印 `slot_roi` 裁剪图 (`cv2.imwrite` 调试） 微调 SLOT_ORIGIN/SLOT_SIZE;OCR 引擎首次运行需下载/加载模型，耐心等。

- [ ] **Step 5: 接入 MapleFarmTask 耗尽保护**

模块级 `DEFAULT_CONFIG` 增加两键：`'药水检查间隔(秒)': 30`、`'药水耗尽保护': True`;`_reset_state()` 增加 `self._last_potion_check = 0.0`（离线测试同源，无需再动）。

`run()` 在第 3 步（喝蓝）之后插入：

```python
        # 3.5 药水耗尽保护(低频 OCR)
        if cfg['药水耗尽保护'] and now - self._last_potion_check >= cfg['药水检查间隔(秒)']:
            self._last_potion_check = now
            hp_count = potions.read_slot_count(frame, self._slot_of(keys['血药键']))
            mp_count = potions.read_slot_count(frame, self._slot_of(keys['蓝药键']))
            empty = farm_logic.potions_exhausted(hp, cfg['喝血阈值'], hp_count,
                                                 mp, cfg['喝蓝阈值'], mp_count)
            if empty:
                self.stop_farming(f'{"血" if empty == "hp" else "蓝"}药耗尽')
                return
```

并加方法（键名转快捷栏槽位；`slot_roi` 对未知键已返回 None，此处直接透传）:

```python
    @staticmethod
    def _slot_of(key_name):
        return key_name.lower()
```

再加 OCR 预热（`on_create` 在任务创建时调用，加载不进 10Hz 热路径）:

```python
    def on_create(self):
        super().on_create()
        if self.config.get('药水耗尽保护'):
            potions.prewarm()
```

文件顶部 import 增加 `from src.detect import potions`。

**best-effort 放行条款**：若 Step 4 中 `TestReadSlotCountRealFrame` 在放大 3 倍后仍持续漏检（PaddleOCR det 对小号描边数字不敏感），允许给该类加 `@unittest.skip('OCR 对小数字漏检,耗尽由喝药无效兜底')` 继续往下走——"药没了"的实际后果已由 Task 5 的 `potion_not_working` 覆盖，OCR 只是让停机原因更精确。跳过必须记录在 commit message。

- [ ] **Step 6: 全量回归 + 补跑 live OCR 测试**

Run: `cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest discover tests -v`
Expected: 全部 PASS(skip 不计失败；游戏在运行时 live 测试也会跑）

```bash
cd /h/ok-mxd/ok-mxd && git add src tests && git commit -m "feat: potion count OCR + exhaustion guard

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: Buff 巡检 buffs.py + 素材采集

**Files:**
- Create: `src/detect/buffs.py`
- Create: `assets/templates/buff_<name>.png`（实弹采集）
- Modify: `src/task/MapleFarmTask.py`(run 内接入 buff 巡检）
- Test: `tests/test_buffs.py`

**Interfaces:**
- Consumes: `farm_logic.parse_buff_config`。
- Produces: `buffs.buff_active(frame, name, threshold=0.8)->bool`、`buffs.load_templates(folder='assets/templates')->dict[str, np.ndarray]`、`buffs.BUFF_AREA`(buff 图标区帧比例 ROI，采集时实测确定）。

- [ ] **Step 1: 采集素材（游戏需在运行，角色已学魔法盾等 buff)**

```bash
# 1) 手动上好所有 buff,立刻截帧
cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" scripts/capture_frame.py screenshots/test_frames/buffs_on.png
# 2) 等 buff 全部消失(或手动取消),再截一帧
"H:/ok-mxd/data/apps/ok-ww/python/python.exe" scripts/capture_frame.py screenshots/test_frames/buffs_off.png
```

然后用裁剪调试脚本定位每个 buff 图标，裁成 ~40x40 PNG 存 `assets/templates/buff_<name>.png`，并把 buff 图标区总范围写入 `buffs.BUFF_AREA`（帧比例）。裁剪可用以下脚本（按输出坐标微调 CROP 常量）:

```python
# scripts/crop_buff_icons.py — 一次性工具,不进 git 也行
import cv2
img = cv2.imread('screenshots/test_frames/buffs_on.png')
# TODO: 按实际 buff 图标位置改
CROP = (0, 0, 40, 40)  # x, y, w, h
x, y, w, h = CROP
cv2.imwrite('assets/templates/buff_magic_shield.png', img[y:y+h, x:x+w])
```

- [ ] **Step 2: 写失败的测试 `tests/test_buffs.py`**

```python
import unittest

import cv2

from src.detect import buffs

ON = 'screenshots/test_frames/buffs_on.png'
OFF = 'screenshots/test_frames/buffs_off.png'


class TestBuffs(unittest.TestCase):

    @classmethod
    def setUpClass(cls):
        cls.on = cv2.imread(ON)
        cls.off = cv2.imread(OFF)
        assert cls.on is not None and cls.off is not None, '先完成素材采集(计划 Task 7 Step 1)'

    def test_active_when_on(self):
        for name in buffs.load_templates():
            self.assertTrue(buffs.buff_active(self.on, name), f'{name} 应激活')

    def test_inactive_when_off(self):
        for name in buffs.load_templates():
            self.assertFalse(buffs.buff_active(self.off, name), f'{name} 应消失')

    def test_missing_template_is_inactive(self):
        self.assertFalse(buffs.buff_active(self.on, 'no_such_buff'))


if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 3: 运行确认失败**

Run: `cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_buffs -v`
Expected: FAIL(`ModuleNotFoundError: src.detect.buffs`)

- [ ] **Step 4: 实现 `src/detect/buffs.py`**

```python
"""Buff 图标模板匹配。模板放 assets/templates/buff_<name>.png。"""
import os

import cv2

# buff 图标区(帧比例),素材采集时实测填写
BUFF_AREA = (0.0, 0.0, 1.0, 0.1)  # TODO: 采集后改精确值

TEMPLATE_FOLDER = 'assets/templates'
_templates = None


def load_templates(folder=TEMPLATE_FOLDER):
    global _templates
    if _templates is None:
        _templates = {}
        if os.path.isdir(folder):
            for f in os.listdir(folder):
                if f.startswith('buff_') and f.endswith('.png'):
                    img = cv2.imread(os.path.join(folder, f))
                    if img is not None:
                        _templates[f[len('buff_'):-len('.png')]] = img
    return _templates


def buff_active(frame, name, threshold=0.8):
    template = load_templates().get(name)
    if template is None:
        return False
    h, w = frame.shape[:2]
    x1, y1 = int(BUFF_AREA[0] * w), int(BUFF_AREA[1] * h)
    x2, y2 = int(BUFF_AREA[2] * w), int(BUFF_AREA[3] * h)
    area = frame[y1:y2, x1:x2]
    if area.size == 0 or area.shape[0] < template.shape[0] or area.shape[1] < template.shape[1]:
        return False
    result = cv2.matchTemplate(area, template, cv2.TM_CCOEFF_NORMED)
    return float(result.max()) >= threshold
```

- [ ] **Step 5: 运行确认通过**

Run: `cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_buffs -v`
Expected: 3 项 PASS。若 `test_inactive_when_off` 误报：提高 threshold 到 0.85 或把模板裁得更紧致（只留图标本体）。

- [ ] **Step 6: 接入 MapleFarmTask**

`DEFAULT_CONFIG` 增加：`'Buff键位': 'magic_shield=q'`（格式 `名称=键,逗号分隔`)、`'Buff巡检间隔(秒)': 10`;`_reset_state()` 增加 `self._last_buff_check = 0.0`（离线测试同源，无需再动）。`run()` 在第 5 步（拾取）后插入：

```python
        # 6. Buff 巡检
        if now - self._last_buff_check >= cfg['Buff巡检间隔(秒)']:
            self._last_buff_check = now
            for buff_name, buff_key in farm_logic.parse_buff_config(cfg['Buff键位']):
                if not buffs.buff_active(frame, buff_name):
                    self.log_info(f'补 buff: {buff_name}')
                    self.send_key(buff_key, after_sleep=1)
```

文件顶部 import 增加 `from src.detect import buffs`。

- [ ] **Step 7: 全量回归 + Commit**

Run: `cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest discover tests -v`
Expected: 全部 PASS

```bash
cd /h/ok-mxd/ok-mxd && git add src assets tests screenshots/test_frames && git commit -m "feat: buff watcher with template matching

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: 兜底守卫（画面静止/经验停滞）

**Files:**
- Create: `src/detect/guards.py`
- Modify: `src/task/MapleFarmTask.py`(run 内接入守卫）
- Test: `tests/test_guards.py`

**Interfaces:**
- Produces: `guards.signature(frame, size=(64, 36))->np.ndarray`（降采样灰度签名）、`guards.frame_frozen(sig_a, sig_b, threshold=0.5)->bool`(mean abs diff < threshold 视为静止）。

说明：未知弹窗/掉线/死亡在 MVP 由"画面静止 60s"与"EXP 10 分钟不涨"兜住（弹窗挡住游戏 → 打不了怪 → EXP 停滞 → 停任务），快速模板识别待素材积累后在 M3 计划补（spec §7 的有意取舍）。

- [ ] **Step 1: 写失败的测试 `tests/test_guards.py`**

```python
import unittest

import numpy as np

from src.detect import guards


class TestGuards(unittest.TestCase):

    def test_identical_frames_frozen(self):
        img = np.random.randint(0, 255, (360, 640, 3), dtype=np.uint8)
        sig = guards.signature(img)
        self.assertTrue(guards.frame_frozen(sig, sig))

    def test_different_frames_not_frozen(self):
        a = np.zeros((360, 640, 3), dtype=np.uint8)
        b = np.full((360, 640, 3), 255, dtype=np.uint8)
        self.assertFalse(guards.frame_frozen(guards.signature(a), guards.signature(b)))

    def test_small_change_still_frozen(self):
        img = np.zeros((360, 640, 3), dtype=np.uint8)
        img2 = img.copy()
        img2[0:2, 0:2] = 30  # 微小扰动
        self.assertTrue(guards.frame_frozen(guards.signature(img), guards.signature(img2)))


if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_guards -v`
Expected: FAIL(`ModuleNotFoundError: src.detect.guards`)

- [ ] **Step 3: 实现 `src/detect/guards.py`**

```python
"""兜底异常检测:画面签名与静止判定。"""
import cv2
import numpy as np


def signature(frame, size=(64, 36)):
    gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
    return cv2.resize(gray, size, interpolation=cv2.INTER_AREA).astype(np.int16)


def frame_frozen(sig_a, sig_b, threshold=0.5):
    return float(np.abs(sig_a - sig_b).mean()) < threshold
```

- [ ] **Step 4: 接入 MapleFarmTask**

`DEFAULT_CONFIG` 增加：`'画面静止上限(秒)': 60`、`'经验停滞上限(分钟)': 10`;`_reset_state()` 增加：

```python
        self._last_sig = None
        self._last_change_time = 0.0
        self._last_exp = None
        self._last_exp_gain_time = 0.0
```

`run()` 在最末尾（buff 巡检后）插入：

```python
        # 7. 兜底守卫
        sig = guards.signature(frame)
        if self._last_sig is None or not guards.frame_frozen(self._last_sig, sig):
            self._last_sig = sig
            self._last_change_time = now
        elif now - self._last_change_time > cfg['画面静止上限(秒)']:
            self.stop_farming('画面长时间静止(卡死/掉线/弹窗)')
            return

        exp = bars.read_exp(frame)
        # 升级后 EXP 条归零:exp 大幅下降同样视为"有收益",复位计时器,否则旧高位卡死计时器必然误停
        if self._last_exp is None or exp > self._last_exp + 0.001 or exp < self._last_exp - 0.05:
            self._last_exp = exp
            self._last_exp_gain_time = now
        elif now - self._last_exp_gain_time > cfg['经验停滞上限(分钟)'] * 60:
            self.stop_farming('经验长时间不涨(无效挂机)')
            return
```

文件顶部 import 增加 `from src.detect import guards`。

注意：`_last_change_time`/`_last_exp_gain_time` 初值 0 会在启动 60s/10min 内误触发，首次赋值后正常——上面的 `is None` 分支已处理首轮。

- [ ] **Step 5: 全量回归 + Commit**

Run: `cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest discover tests -v`
Expected: 全部 PASS

```bash
cd /h/ok-mxd/ok-mxd && git add src tests && git commit -m "feat: frozen-frame and exp-stall guards

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 9: YOLO 数据集采集与标注

**Files:**
- Create: `scripts/record_frames.py`
- Create: `dataset/raw/*.png`(采集)、`dataset/images/{train,val}/*.png`、`dataset/labels/{train,val}/*.txt`、`dataset/mobs.yaml`

**Interfaces:**
- Produces: YOLO 格式数据集（Task 10 消费）;`dataset/mobs.yaml` 的类别清单（Task 11 的 `dic_labels` 必须与之严格一致）。

**标注约定：** 单一类别 `mob`（所有怪物一类，MVP 只求"有怪/无怪"；其他玩家、宠物、NPC 不标 = 负样本）。YOLO txt 每行 `0 <cx> <cy> <w> <h>`（归一化）。**train/val 按"采集场次/地图"整块切分，禁止随机切**——同图连拍的相邻帧近乎重复，随机切会把近同一张图同时放进 train 和 val,val mAP 虚高失去意义（至少留一张图的 30 帧做 holdout)。

- [ ] **Step 1: 写采集脚本 `scripts/record_frames.py`**

```python
"""每隔 N 秒抓一帧存入 dataset/raw/<地图名>/,站到刷怪图挂机采集。
用法: python scripts/record_frames.py <地图名> [间隔秒=2] [数量=50]"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import time

from scripts.capture_frame import build_capture

if __name__ == '__main__':
    import cv2

    map_name = sys.argv[1] if len(sys.argv) > 1 else 'map'
    interval = float(sys.argv[2]) if len(sys.argv) > 2 else 2.0
    count = int(sys.argv[3]) if len(sys.argv) > 3 else 50
    out_dir = f'dataset/raw/{map_name}'
    os.makedirs(out_dir, exist_ok=True)
    ev, win, cap = build_capture()
    existing = len(os.listdir(out_dir))
    for i in range(count):
        frame = cap.get_frame()
        if frame is not None:
            path = f'{out_dir}/frame_{existing + i:04d}.png'
            cv2.imwrite(path, frame)
            print(f'{i + 1}/{count} {path}')
        time.sleep(interval)
    ev.set()
```

- [ ] **Step 2: 采集（游戏需在刷怪图，角色正常打怪/走位）**

```bash
cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" scripts/record_frames.py <地图名> 2 50
```

**至少 3 张不同地图**，每张 50 帧（共 ~150 帧），按地图分目录存放。通过判据：抽看 10 帧，怪物清晰、有遮挡/重叠/远近变化，含少量无怪帧（负样本）；怪种类超过 3 种时，每种至少 30 帧含它。只采一张图不许往下走（模型只会认那张图）。

- [ ] **Step 3: 标注 → YOLO txt（工具化，不靠 Claude 视觉逐框手写）**

1500 个框靠视觉模型手写归一化坐标既不精也烧 token，用工具：

```bash
# 若 yolo-venv 尚未创建(Task 10 也会用它),先建
"H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m venv /h/ok-mxd/yolo-venv
/h/ok-mxd/yolo-venv/Scripts/python.exe -m pip install labelImg
/h/ok-mxd/yolo-venv/Scripts/labelImg.exe dataset/raw dataset/classes.txt
```

（`dataset/classes.txt` 内容一行 `mob`;labelImg 设成 YOLO 格式，逐图标框。)

两种走法二选一：
- **全人工**:150 帧约 1-2 小时，质量最可靠
- **自举（省力，推荐）**：人工标 30 帧（3 图各 10)→ 提前跑一次 Task 10 训弱模型 → 用弱模型自动标剩余帧 → labelImg 里逐帧纠错（纠远比标快）

产出归位：`dataset/images/{train,val}/`、`dataset/labels/{train,val}/`，**按地图整块分 train/val**(holdout 地图整图进 val)。

- [ ] **Step 3.5: 标注质检（交付脚本 `scripts/draw_yolo_boxes.py`)**

```python
"""把 YOLO 标注画回图上抽查。用法: python scripts/draw_yolo_boxes.py <图片路径>"""
import os
import sys

import cv2

if __name__ == '__main__':
    img_path = sys.argv[1]
    txt = img_path.replace('images', 'labels').replace('.png', '.txt')
    img = cv2.imread(img_path)
    h, w = img.shape[:2]
    if os.path.exists(txt):
        for line in open(txt, encoding='utf-8'):
            _, cx, cy, bw, bh = map(float, line.split())
            x1, y1 = int((cx - bw / 2) * w), int((cy - bh / 2) * h)
            x2, y2 = int((cx + bw / 2) * w), int((cy + bh / 2) * h)
            cv2.rectangle(img, (x1, y1), (x2, y2), (0, 0, 255), 2)
    out = img_path.replace('.png', '_boxed.png')
    cv2.imwrite(out, img)
    print(out)
```

通过判据：对 train/val 各抽 5 帧画框回看，框紧贴怪本体，不漏不歪。

- [ ] **Step 4: 写 `dataset/mobs.yaml`**

```yaml
path: H:/ok-mxd/ok-mxd/dataset
train: images/train
val: images/val
names:
  0: mob
```

- [ ] **Step 5: Commit**

```bash
cd /h/ok-mxd/ok-mxd && git add scripts dataset/mobs.yaml && echo 'dataset/raw/
dataset/images/
dataset/labels/' >> .gitignore && git add .gitignore && git commit -m "feat: YOLO dataset pipeline + mobs.yaml

Co-Authored-By: Claude <noreply@anthropic.com>"
```

（原始帧与标注量大不入库；`mobs.yaml` 与脚本入库。）

---

### Task 10: YOLO 训练与导出 ONNX

**Files:**
- Create: `H:\ok-mxd\yolo-venv\`（独立训练环境，**不污染 ok-ww 嵌入式 Python**)
- Create: `assets/mob_model/mob.onnx`（训练产物，拷入 ok-mxd)

**Interfaces:**
- Consumes: Task 9 的 `dataset/mobs.yaml`。
- Produces: `assets/mob_model/mob.onnx`(Task 11 加载）。

- [ ] **Step 1: 建训练环境并装 ultralytics**

```bash
"H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m venv /h/ok-mxd/yolo-venv
/h/ok-mxd/yolo-venv/Scripts/python.exe -m pip install ultralytics
```

注：会下载 torch(CPU 版 ~200MB)，需联网。有 N 卡可改装 cu 版 torch 加速。若 venv 已在 Task 9 Step 3 创建过，跳过建环境直接补装 ultralytics。

- [ ] **Step 1.5: 定 imgsz（先量怪缩放后的尺寸）**

一只蜗牛在原图约 60x50 px；缩到 640 宽后只剩 ~15x12 px，对 yolov8n 是勉强尺度。量法：从 `dataset/labels/train` 取若干标注，`bw * imgsz`(bw 为归一化宽）即为缩放后像素宽。

```bash
cd /h/ok-mxd/ok-mxd && "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -c "
import glob
ws = []
for p in glob.glob('dataset/labels/train/*.txt'):
    for line in open(p, encoding='utf-8'):
        ws.append(float(line.split()[3]))
ws.sort()
print('标注宽(归一化) p10/p50/p90:', ws[len(ws)//10], ws[len(ws)//2], ws[len(ws)*9//10])
print('imgsz=640  时像素宽:', [round(x*640, 1) for x in (ws[len(ws)//10], ws[len(ws)//2])])
print('imgsz=1280 时像素宽:', [round(x*1280, 1) for x in (ws[len(ws)//10], ws[len(ws)//2])])
"
```

判据：p10（最小的一成怪）缩放后 ≥ 20px 选 640，否则选 1280(CPU 训练时间约翻 3-4 倍，可接受）。**选定值记为 IMGSZ，后续训练、导出、检测器 model_h/model_w 四处必须一致。**

- [ ] **Step 2: 训练**

```bash
cd /h/ok-mxd/ok-mxd && /h/ok-mxd/yolo-venv/Scripts/python.exe -c "from ultralytics import YOLO; YOLO('yolov8n.pt').train(data='dataset/mobs.yaml', epochs=100, imgsz=IMGSZ, project='dataset/runs', name='mob')"
```

Expected:`dataset/runs/mob/weights/best.pt`。通过判据：训练日志末尾 val mAP50 ≥ 0.7(val 是整图 holdout，不含近重复帧，此分数可信）；不达标先加数据（回 Task 9 Step 2 补采困难样本）再训，不许降标准放行。

- [ ] **Step 3: 导出 ONNX 并拷入项目**

```bash
cd /h/ok-mxd/ok-mxd && /h/ok-mxd/yolo-venv/Scripts/python.exe -c "from ultralytics import YOLO; YOLO('dataset/runs/mob/weights/best.pt').export(format='onnx', imgsz=IMGSZ)"
mkdir -p assets/mob_model && cp dataset/runs/mob/weights/best.onnx assets/mob_model/mob.onnx
```

- [ ] **Step 4: Commit**

```bash
cd /h/ok-mxd/ok-mxd && git add assets/mob_model && git commit -m "feat: trained mob detection model (yolov8n onnx)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 11: YOLO 运行时接入（检测驱动攻击）

**Files:**
- Create: `src/OpenVinoYolo8Detect.py`（从 ok-ww 复制适配。**必须用 OpenVino 版**:ok-ww 嵌入式 Python 无 onnxruntime 有 openvino（已实测）,OnnxYolo8Detect 顶部 `import onnxruntime` 必炸；OpenVinoYolo8Detect 的 `core.read_model()` 直接吃 .onnx，训练产物不用改）
- Modify: `src/globals.py`（加 yolo_model/yolo_detect)、`src/task/BaseMapleTask.py`（加 find_mobs)、`src/task/MapleFarmTask.py`（攻击模式）
- Test: `tests/test_yolo.py`、`tests/test_farm_task_offline.py`（加检测模式用例）

**Interfaces:**
- Consumes: `assets/mob_model/mob.onnx`;ok-ww 的 `og.my_app.yolo_detect(image, threshold, label)->list[Box]` 模式。
- Produces: `Globals.yolo_detect(image, threshold=0.5, label=-1)->list[Box]`;`BaseMapleTask.find_mobs(frame=None, threshold=0.5)->list[Box]`;`MapleFarmTask` 新配置 `'攻击模式'`(`'检测'`/`'定频'`)、`'攻击区宽'`/`'攻击区高'`/`'攻击区中心X'`/`'攻击区中心Y'`（帧比例）。

- [ ] **Step 1: 复制适配 `src/OpenVinoYolo8Detect.py`**

```bash
cp /h/ok-mxd/data/apps/ok-ww/repo/src/OpenVinoYolo8Detect.py /h/ok-mxd/ok-mxd/src/OpenVinoYolo8Detect.py
```

改 `__init__`(line 14-15）中 `self.dic_labels = {0: 'echo'}` 为 `self.dic_labels = {0: 'mob'}`（必须与 `dataset/mobs.yaml` 一致）;`model_h`/`model_w` 默认值改为 Task 10 选定的 IMGSZ。

- [ ] **Step 2: `src/globals.py` 增加模型加载（全量替换 Task 1 的桩）**

```python
import os

from PySide6.QtCore import QObject

from ok import Logger, get_path_relative_to_exe

logger = Logger.get_logger(__name__)


class Globals(QObject):
    """应用级单例(对应 config['my_app'])。"""

    def __init__(self, exit_event):
        super().__init__()
        self.exit_event = exit_event
        self._yolo_model = None

    @property
    def yolo_model(self):
        if self._yolo_model is None:
            weights = get_path_relative_to_exe(os.path.join('assets', 'mob_model', 'mob.onnx'))
            from src.OpenVinoYolo8Detect import OpenVinoYolo8Detect
            self._yolo_model = OpenVinoYolo8Detect(weights=weights)
        return self._yolo_model

    def yolo_detect(self, image, threshold=0.5, label=-1):
        return self.yolo_model.detect(image, threshold=threshold, label=label)
```

- [ ] **Step 3: `BaseMapleTask` 增加 find_mobs**

```python
    def find_mobs(self, frame=None, threshold=0.5):
        from ok import og
        return og.my_app.yolo_detect(frame if frame is not None else self.frame,
                                     threshold=threshold, label=0)
```

- [ ] **Step 4: 写测试 `tests/test_yolo.py`（模型存在才跑）**

```python
import os
import unittest

import cv2

from src.OpenVinoYolo8Detect import OpenVinoYolo8Detect

MODEL = 'assets/mob_model/mob.onnx'


@unittest.skipUnless(os.path.exists(MODEL), 'YOLO 模型未训练(Task 10),跳过')
class TestYolo(unittest.TestCase):

    @classmethod
    def setUpClass(cls):
        cls.detector = OpenVinoYolo8Detect(weights=MODEL)

    def test_detects_mobs_in_annotated_frame(self):
        """从 dataset/images/val 取一张有标注的帧,检出数应与标注数接近。"""
        import glob
        labeled = [p for p in glob.glob('dataset/labels/val/*.txt') if os.path.getsize(p) > 0]
        self.assertTrue(labeled, 'val 集无正样本标注')
        txt = sorted(labeled)[0]
        frame = cv2.imread(txt.replace('labels', 'images').replace('.txt', '.png'))
        expected = sum(1 for _ in open(txt, encoding='utf-8'))
        boxes = self.detector.detect(frame, threshold=0.5, label=0)
        self.assertGreaterEqual(len(boxes), max(1, expected - 1),
                                f'标注 {expected} 只,仅检出 {len(boxes)} 只')

    def test_empty_frame_no_mob(self):
        import glob
        empty = [p for p in glob.glob('dataset/labels/val/*.txt') if os.path.getsize(p) == 0]
        if not empty:
            self.skipTest('val 集无负样本')
        frame = cv2.imread(sorted(empty)[0].replace('labels', 'images').replace('.txt', '.png'))
        boxes = self.detector.detect(frame, threshold=0.5, label=0)
        self.assertEqual(len(boxes), 0, f'负样本误报 {len(boxes)} 个')


if __name__ == '__main__':
    unittest.main()
```

Run: `cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest tests.test_yolo -v`
Expected: 2 项 PASS（模型未就绪时 SKIP)。误报多则提高 threshold 到 0.6 或回 Task 9 补负样本。

- [ ] **Step 5: MapleFarmTask 接入检测驱动攻击**

`DEFAULT_CONFIG` 增加：`'攻击模式': '定频'`（可选 `'检测'`，模型就绪后用户切换）、`'攻击区宽': 0.6`、`'攻击区高': 0.6`、`'攻击区中心X': 0.5`、`'攻击区中心Y': 0.5`（帧比例，**用户按自己站桩点标定一次**——镜头在地图边缘会夹住，角色并不总在画面中心）。`run()` 第 4 步（定频攻击）整段替换为：

```python
        # 4. 攻击
        if cfg['攻击模式'] == '检测':
            if farm_logic.should_attack(now, self._last_attack, cfg['攻击间隔(秒)']):
                mobs = self.find_mobs(frame)
                if self._mob_in_attack_zone(frame, mobs, cfg):
                    self.send_key(keys['攻击键'])
                    self._last_attack = now
        elif farm_logic.should_attack(now, self._last_attack, cfg['攻击间隔(秒)']):
            self.send_key(keys['攻击键'])
            self._last_attack = now
```

并加方法：

```python
    @staticmethod
    def _mob_in_attack_zone(frame, mobs, cfg):
        """怪物检测框中心落在攻击区内。攻击区以用户标定的站桩点(攻击区中心X/Y)为锚。"""
        h, w = frame.shape[:2]
        half_w = cfg['攻击区宽'] / 2 * w
        half_h = cfg['攻击区高'] / 2 * h
        cx, cy = cfg['攻击区中心X'] * w, cfg['攻击区中心Y'] * h
        for mob in mobs:
            mx, my = mob.x + mob.width / 2, mob.y + mob.height / 2
            if abs(mx - cx) <= half_w and abs(my - cy) <= half_h:
                return True
        return False
```

- [ ] **Step 5.5: 检测模式离线测试（补进 `tests/test_farm_task_offline.py`)**

```python
    def test_detect_mode_attacks_when_mob_in_zone(self):
        task = make_task(**{'攻击模式': '检测'})
        mob = MagicMock(x=1200, y=700, width=60, height=50)  # 中心 (1230,725),在默认攻击区内
        task.find_mobs = MagicMock(return_value=[mob])
        run_with_frame(task)
        self.assertIn(call('shift'), task.send_key.call_args_list)

    def test_detect_mode_idles_when_no_mob(self):
        task = make_task(**{'攻击模式': '检测'})
        task.find_mobs = MagicMock(return_value=[])
        run_with_frame(task)
        task.send_key.assert_not_called()  # 无怪停手省蓝

    def test_detect_mode_idles_when_mob_outside_zone(self):
        task = make_task(**{'攻击模式': '检测'})
        far = MagicMock(x=10, y=10, width=60, height=50)  # 左上角,攻击区外
        task.find_mobs = MagicMock(return_value=[far])
        run_with_frame(task)
        task.send_key.assert_not_called()
```

- [ ] **Step 6: 实弹验证（游戏需在刷怪图）**

启用任务，先把 `'攻击模式'` 保持 `'定频'` 确认模型加载不报错（日志无 yolo 异常），再切 `'检测'`：有怪时角色攻击、怪清完停手、刷新后再打。
通过判据：日志可见检测帧耗时 < 200ms；停手期间不放技能（省蓝生效）。

- [ ] **Step 7: 全量回归 + Commit**

Run: `cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest discover tests -v`
Expected: 全部 PASS

```bash
cd /h/ok-mxd/ok-mxd && git add src tests assets && git commit -m "feat: YOLO-driven attack (detect mode with cadence fallback)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 12: 收尾验收 + README

**Files:**
- Create: `README.md`
- Modify: `H:\ok-mxd\docs\superpowers\specs\2026-08-05-ok-mxd-design.md`（里程碑勾选 M0/M1/M2/M3 完成项；spec v3 编号即 M3=YOLO/M4=GUI，与计划一致）

**Interfaces:**
- Consumes：全部前序任务。

- [ ] **Step 1: 全量测试**

Run: `cd /h/ok-mxd/ok-mxd && PYTHONIOENCODING=utf-8 "H:/ok-mxd/data/apps/ok-ww/python/python.exe" -m unittest discover tests -v`
Expected: 全部 PASS（游戏运行时含 live 测试）

- [ ] **Step 2: GUI 验收检查单（游戏在运行）**

启动 `main_debug.py`，逐项确认：
- [ ] 触发任务页出现"自动打怪"卡片，可启用/停用
- [ ] 设置页可见"游戏按键"配置，改动后重启仍保留（写入了 `configs/`)
- [ ] 任务配置（攻击间隔/阈值等）改动即时生效
- [ ] F9 全局急停生效
- [ ] 启用后角色正常打怪 5 分钟：攻击节拍、喝药、buff 补放、日志无刷错
- [ ] 拔掉一个前提（如把血药阈值调到 1.0）验证"连续喝药无效"或耗尽保护能停任务（可选）

- [ ] **Step 3: 写 README.md**

内容：项目简介 + 账号风险声明（照抄 config.py about)+ 使用步骤（启动游戏→2560x1440→站位→启动 main_debug.py→启用任务）+ **运行约束：只在 2560x1440 挂机（主循环有尺寸守卫）；挂机期间不得拖动/缩放/折叠游戏内 UI 面板、不得改分辨率** + Task 2 Step 4.5 的分辨率缩放实验结论（UI 是固定像素还是随分辨率缩放）+ 按键配置说明 + 常见问题（PostMessage 无效则按 Task 2 两级降级，降级后游戏须前台）。

- [ ] **Step 4: 更新 spec 里程碑 + Commit**

把 spec §8 的 M0/M1/M2/M3 标记完成（M0-3 PostMessage 记录实测结论）,M4 保持待办。

```bash
cd /h/ok-mxd/ok-mxd && git add -A && git commit -m "docs: README and acceptance

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Self-Review 记录

- Spec 覆盖：M0 五项 → Task 2（含判据与 Step 4.5 分辨率实验）;M1 → Task 1/3/4/5;M2(buff/拾取/兜底） → Task 6/7/8;M3(YOLO) → Task 9/10/11；账号风险声明 → Task 1(config about)+ Task 12(README)；运行约束 → Task 12。spec §7"未知弹窗模板识别"有意降级为 EXP 停滞兜底（Task 8 说明）。
- **有意删减（防执行者误当遗漏）**:① spec §4/§5 曾有的"小地图红点怪物检测"已整体移除（2026-08-05 实测证伪：怀旧服小地图不画怪），怪物检测由 Task 9-11 的 YOLO 承担，定频攻击是模型就绪前的降级；② 拾取从 spec 的"左右各走一步+按键×3"简化为"定时按一次拾取键"（走位拾取有掉平台风险，默认关闭靠宠物）;③ 死亡检测不走"静止守卫"（死亡弹窗后背景动画仍在）,Task 5 用"连续 N 帧空血"专判。
- 类型一致性：`read_hp/read_mp/read_exp`(bars ↔ BaseMapleTask ↔ MapleFarmTask)、`parse_buff_config`(farm_logic ↔ Task 7)、`potions_exhausted` 参数序（farm_logic ↔ Task 6 调用）、`yolo_detect(image, threshold, label)`(Globals ↔ find_mobs ↔ OnnxYolo8Detect)、`dic_labels` ↔ `mobs.yaml` names、`DEFAULT_CONFIG`/`_reset_state()`(MapleFarmTask ↔ 离线测试）已核对一致。
- 已验证结论（评审实测）:EXP 条满刻度 1357-1571、填充 1357-1508、填充色 r 206→147 渐变（像素级游程实测，2026-08-05 第三轮评审纠正——此前"1552 已验证"的结论作废，当时用的 r≥170 掩码在渐变尾断裂，填充终点量偏）;MP 左边界 1145;HP 943-1139 读数 1.0 ✓;`'ratio': None` 安全（parse_ratio 返回 None,out_of_ratio 恒 False);`PostMessageInteraction(cap, win)`、`HwndWindow(...)`、`WindowsGraphicsCaptureMethod(win)` 构造签名已核对；`shift/ctrl/home/insert/delete/pageup/esc/z` 键名有效；**嵌入式 Python 无 onnxruntime 有 openvino**(YOLO 检测器必须复制 OpenVinoYolo8Detect.py，其 read_model 直接吃 .onnx)。
- 已知风险（执行时处理）:①onnxocr 直读 API 形态（`ONNXPaddleOcr`）与框架封装可能有出入，Task 6 Step 4 有调试指引与 best-effort 放行条款；②BUFF_AREA 常量待采集时实测；③`test_postmessage_response` 若失败必须先人眼复核 m0_fail_*.png 再下降级结论（防假阴性）。
