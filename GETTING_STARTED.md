# ok-mxd 新手配置指南 · 注意事项

> 面向**第一次使用**的人。读完这一篇就能跑起来。
> 想了解「采集 → 标注 → 训练 → 换地图重训模型」的完整链路，看 [`OPERATION_GUIDE.md`](OPERATION_GUIDE.md)。
> 命令都在项目根目录 `H:\ok-mxd\ok-mxd` 下执行。

---

## ⚠️ 先看这一段

1. **本项目仅供个人学习 Python / 计算机视觉 / UI 自动化。使用它可能导致账号被封禁，后果自负。**
2. 这是**前台挂机**：游戏窗口必须一直在最前，挂机期间你**不能碰键盘鼠标**（碰了按键就打到别的窗口去了）。
3. **急停 = `F9`**（全局热键，任何时候都能按）。先记住这个键再往下看。

---

## 一、硬前置条件（不满足就一定跑不起来）

| 条件 | 说明 | 不满足的后果 |
|---|---|---|
| 游戏是**冒险岛怀旧服** | 进程 `Maplestory_Classic.exe`、窗口类 `UnityWndClass`、标题含「冒险岛怀旧服」 | 连不上窗口 |
| 分辨率**必须 2560x1440** | 代码里有帧尺寸守卫，连续 10 帧不符自动停 | 任务自动停；就算绕过，所有 ROI 也全偏（UI 随分辨率缩放，HP 条 200px@1440p vs 约 110px@1080p） |
| **以管理员身份运行** | 按键走 `PyDirect`（SendInput 驱动级） | 非管理员被 BlackCipher 拦截，报 error 5，一个键也发不出去 |
| 游戏窗口**保持前台** | 后台方案（PostMessage）2026-08-05 实测三级全灭：非管理员 error 5，管理员发送"成功"但被静默忽略 | 键发出去了但游戏收不到，角色站着不动 |

> 后台挂机、多开挂机、最小化挂机：**都不支持**，不用试，已经实测过了。

---

## 二、游戏内准备（做一次就行）

1. 分辨率调到 **2560x1440**。
2. 把要用的东西**拖到快捷栏**，并记住对应按键：
   - 红药（血药）
   - 蓝药
   - 攻击技能
   - 宠物食品（想自动喂宠物才需要）
   - 椅子（想没怪时自动坐下回血蓝才需要）
   - 回城卷（想低血自动逃跑才需要）
3. 记下你的**角色名**，一个字都不能差（后面要填进配置，程序靠 OCR 找角色脚下的名字牌来定位）。
4. 站到要挂机的地图上，**别把游戏内 UI 面板拖来拖去**（血蓝条、快捷栏位置是按标定好的 ROI 读的）。

默认按键（可在 GUI 改，改完写进 `configs/游戏按键.json`）：

| 功能 | 默认键 |
|---|---|
| 攻击键 | `ctrl` |
| 血药键 | `home` |
| 蓝药键 | `insert` |
| 拾取键 | `z` |
| 左移 / 右移 | `left` / `right` |
| 宠物食物键 | 留空（不喂） |
| 椅子键 | 留空（不坐） |
| 回城卷键 | 留空（低血只停任务，不逃跑） |

---

## 三、启动

### 方式 A：双击（推荐新手）

双击项目根目录的 **`elevated_launch.cmd`**。它会自己弹 UAC 提权、提醒你游戏没开、退出时把测试用存档帧补回来。

### 方式 B：管理员 PowerShell

```powershell
# PowerShell 图标右键 →「以管理员身份运行」
cd H:\ok-mxd\ok-mxd
& "H:\ok-mxd\data\apps\ok-ww\python\python.exe" main_debug.py
```

> Python **只能**用 `H:\ok-mxd\data\apps\ok-ww\python\python.exe`（ok-ww 框架自带的嵌入式解释器，已装好 PySide6 / OpenCV / OpenVINO）。
> **不要往这个解释器里 `pip install`**。要装 ultralytics 做训练/标注，用另一套环境 `H:\ok-mxd\yolo-venv`。

### GUI 里的三步

1. **Start 页** → 点启动（连接游戏窗口，看到画面预览说明连上了）
2. **设置页 →「游戏按键」** → 逐个核对与游戏内一致
3. **触发任务页** → 启用「**自动打怪**」

另外有个「**战士调试**」任务，只画框不按键，可以和「自动打怪」同时开着看可视化（玩家框绿、攻击区蓝/有怪变红、怪物框黄、怪脚底点青）。第一次调攻击区时开着它很有用。

---

## 四、第一次必须调的 4 个配置

在触发任务页点开「自动打怪」的配置：

| 配置 | 怎么填 |
|---|---|
| **角色名** | 与游戏内**完全一致**。留空的话攻击区会锚在画面正中央，等于没定位 |
| **攻击模式** | 默认 `检测`（YOLO 看到攻击区里有怪才打）。`定频` = 无脑按固定节奏，只在模型不好使时退回去用 |
| **攻击区宽(像素)** | 默认 600。实测 **800** 是当前地图较好的值；`单体(面朝)` 形状下**射程 = 宽的一半** |
| **攻击区形状** | 默认 `单体(面朝)`。魔法箭/近战这类朝向性技能选它；`群体(对称)` 是安全退路 |

调完用标定脚本看图确认（**不要凭感觉调**）：

```powershell
& "H:\ok-mxd\data\apps\ok-ww\python\python.exe" scripts\calibrate_attack_zone.py `
    --frame <一张2560x1440截图> --name <角色名> --width 800 --height 200 --offset 90
```

输出图默认写到 `screenshots\calibrate_attack_zone.png`。看红框有没有正好套住你的名字牌、攻击区在不在合理位置，不对就改 `--width/--height/--offset` 再跑一次，满意了把同样的数填进 GUI。
没有现成截图就先 `scripts\capture_frame.py` 抓一张。

> **攻击区高不要加。** 实测同层怪的框中心全落在锚点 `-105..-2` 像素范围，现行高=200（覆盖 `-188..+12`）对同层怪覆盖率**已经是 100%**，加高只会把够不着的邻近平台的怪拉进来，让角色挥空。

---

## 五、配置项全表

### 攻击

| 配置 | 默认 | 说明 |
|---|---|---|
| 攻击模式 | 检测 | `检测` / `定频` |
| 攻击间隔(秒) | 1.5 | 攻击键**轻点**节奏；检测模式下同时是完整检测拍（锚点 OCR + YOLO）的节流。法师建议 0.7 左右 |
| 攻击区形状 | 单体(面朝) | 单体：只打面朝侧半区；群体：打整个矩形 |
| 攻击区宽 / 高(像素) | 600 / 200 | 见上一节。**高别动** |
| 丢怪保持(秒) | 1.0 | 攻击区里检测不到怪之后还按「有怪」继续挥多久。**这是「打两下就停手」的解**（实测攻击段中位时长 1.31s → 2.08s）。必须大于一个攻击间隔，设 0 = 关掉 |
| 转向冷却(秒) | 1.5 | 两次转向的最小间隔，防止怪在身体左右反复横跳时角色原地扭。设 0 = 关掉 |
| 朝向 | 自动 | 走位结束后面朝哪边，可显式指定左/右 |

### 血蓝与安全（**最重要，见第六节雷区**）

| 配置 | 默认 | 说明 |
|---|---|---|
| 喝药开关 | true | 总开关，关掉后完全不喝血蓝 |
| 喝血阈值 / 喝蓝阈值 | 0.7 / 0.35 | **0-1 的比例**，不是百分数 |
| 喝药判定间隔(秒) | 1.0 | 两次喝药/判效的最小间隔，太短会误判「喝药无效」 |
| 喝药无效上限 | 5 | 连喝 N 次血不涨 → 停任务（药大概率没了） |
| 保命血线 | 0.25 | 低于它：喝血 → 有回城卷就回城 → 停任务 |
| 死亡判定线 / 死亡确认帧数 | 0.02 / 20 | 连续 N 帧空血判定死亡 |
| 药水检查间隔(秒) / 药水耗尽保护 | 30 / true | 低频 OCR 查快捷栏药水数量 |

### 兜底守卫（别关）

| 配置 | 默认 | 触发后 |
|---|---|---|
| 画面静止上限(秒) | 60 | 帧签名一直不变 → 停（掉线/卡死） |
| 经验停滞上限(分钟) | 10 | 经验条这么久不涨 → 停（无效挂机）。**屏幕上一只怪都没有的时段不计入**（空图/刷新间隙不该被判停），仅检测模式有此豁免 |

### 定位与寻怪

| 配置 | 默认 | 说明 |
|---|---|---|
| 名字牌到身体偏移(像素) | 90 | 名字牌中心 → 身体中心的距离，标定脚本能看出来 |
| 锚点搜索区宽/高/中心Y(比例) | 0.30 / 0.30 / 0.55 | OCR 找名字牌的搜索区。日志反复出现「未定位到角色」（贴地图边缘站桩常见）就调大 |
| 锚点刷新间隔 / 保鲜(秒) | 2 / 10 | |
| 模板分片匹配开关 | true | 名字牌被怪挡住也能定位的快通道（OCR 命中一次后自动建模板存 `screenshots/nametag_templates/`）。**「怪堆里一直寻怪不攻击」的主要解法，别关** |
| 模板匹配阈值 | 0.2 | 归一化平方差，越小越严 |
| 寻怪开关 | true | 同层有怪但都在攻击区外时自动走近（仅检测模式） |
| 寻怪同层容差(像素) | 60 | 高度差在此范围内才算同层，避免追到别的平台上 |
| 寻怪刷新间隔(秒) | 0.4 | 越小接战越快，但 YOLO 跑得越勤、决策日志写得越密 |
| `空闲刷新间隔(秒)` | 0.3 | 不打也不追时的检测节奏。**起步寻怪只能在检测拍里发生,这个值直接决定「停手到迈腿」有多快**。调回 0.7 = 修复前行为 |
| 寻怪外推速度(像素/秒) | 250 | 名字牌被挡、OCR 连续失败时按此速度水平外推攻击区 |
| 玩家宽/高(像素) | 60 / 120 | **只影响调试画框**，不影响攻击判定 |

### 辅助

| 配置 | 默认 | 说明 |
|---|---|---|
| 拾取开关 / 拾取间隔(秒) | false / 30 | 默认关（走位拾取有掉平台风险，靠宠物捡） |
| 喂宠物开关 / 间隔(秒) | true / 900 | 需要先在游戏内把宠物食物拖到快捷栏并绑好键 |
| 坐椅开关 / 坐椅延迟(秒) | true / 3.0 | 检测模式没怪闲置超过延迟就坐下回血蓝；椅子键留空则不坐 |
| 走位开关 / 间隔 / 持续时间(秒) | true / 120 / 0.4 | 防挂机检测的小位移 |
| **决策日志开关** | false | 排查用，见第七节。**排完记得关** |

---

## 六、雷区（真踩过，逐条看）

### 1. 所有阈值都是 0-1 的比例，不是百分数

`喝血阈值` 填 `5.2` 会怎样：`need_hp_potion(hp, 5.2)` 判的是 `hp < 5.2` → **恒为真** → 满血也每 3 秒按一次药键 → HP 连续 3 次不涨 → 约 9 秒后触发「连续喝药无效」直接停任务。

反过来填 `0.0` 也一样致命：`hp < 0.0` **永不成立** → **永远不喝药**。

`保命血线`、`死亡判定线` 同理，填 0 = 这道保险彻底失效。

### 2. 不要把三道保险同时关掉

2026-08-07 03:45 真实事故：用户把 `死亡判定线=0.0`（死亡检测永不成立）、`保命血线=0.0`（保命永不成立）、`喝药开关=false`、回城卷键留空 —— 结果角色 03:37 就死了，框架**完全不知道**，锚点在同一像素点冻结了 3.3 分钟，直到 03:45 才被「经验停滞」守卫兜住，白挂 8 分钟。

> **锚点像素级冻结 + 周围怪照常游动 = 角色已死/卡死的特征签名。**
> 目前「经验停滞」守卫是唯一还生效的兜底，**不要关它**。

⚠️ 仓库里现存的 `configs/MapleFarmTask.json` 就是事故后那份配置的变体：`喝药开关=false`、`喝血阈值=0.0`、`保命血线=0.0`。第一次用之前请按第五节的默认值改回来。

### 3. 前台挂机的日常约束

- 挂机期间**不要碰键盘鼠标**、不要切窗口、不要让别的窗口弹到前台
- 不要拖动 / 缩放 / 折叠游戏内 UI 面板，不要改分辨率（改了任务会自动停）
- 想停就按 **`F9`**，或在 GUI 里停用任务

### 4. GUI 启动会清空 `screenshots/` 目录

这是框架行为。`screenshots/test_frames/training_ground_full_2560x1440.png` 这张存档测试帧**已经整个进了 `.gitignore`，`git checkout` 恢复不了**，备份在 `H:\ok-mxd\_frames_backup\`。

`tests/test_bars.py`、`tests/test_potions.py`、`tests/test_farm_task_offline.py` 三个模块都依赖它，丢了会批量假失败（测试 0.3 秒就跑完 = 就是这个症状）。用 `elevated_launch.cmd` 启动的话，它退出时会自动从备份补回来。

### 5. 改了代码要重启 GUI（不是所有改动都热重载）

框架的调试文件监视器只 `importlib.reload` 任务模块自身，**不递归重载依赖**。所以：

- 只改 `src/task/MapleFarmTask.py` → 热重载生效
- 改了 `src/detect/*` 或 `src/task/farm_logic.py` → **必须重启 GUI**，否则「新任务代码 + 旧依赖模块」会报 `AttributeError`

### 6. 决策日志别长期开着

`决策日志开关` 打开后，每个检测拍往 `logs/ok-script.log` 写一行。配合 `寻怪刷新间隔=0.1` 就是**每秒 10 行**，日志能涨到十几 MB。排查完记得关。

### 7. 攻击区调大不会更好

实测数据：宽 800 → 1007 之后，转向频率从 0.090 涨到 0.126 次/秒（更频繁地原地左右扭），因为加宽 = 更多怪在边界进进出出。**加宽/加高都不是解**。区内长期只有 0-1 只怪是地图本身怪稀疏（645 次检测里只有 23% 是同层怪，同层怪水平距离中位 616px），不是标定错了。

### 8. 日志在哪

实际写在 **`logs/ok-script.log`**（不吃 `config.py` 里的 `log_file` 设置）。任务自动停了先看它：死亡 / 低血 / 药尽 / 画面静止 / 经验停滞 / 分辨率不符，都会写明原因。

---

## 七、出问题怎么查

| 症状 | 按顺序查 |
|---|---|
| **按键完全无效，角色不动** | ① 是不是管理员运行？② 游戏窗口在前台吗？③ 攻击键与游戏内一致吗（默认 `ctrl`，不是 `shift`） |
| **检测模式不攻击** | ① `角色名` 与游戏内完全一致？② 跑 `calibrate_attack_zone.py` 看红框有没有套住名字牌 ③ 日志里反复「未定位到角色」→ 调大「锚点搜索区宽/高(比例)」④ 攻击区是不是太小 |
| **打两下就停手 / 施法放不出来** | 调大 `丢怪保持(秒)`（要大于一个攻击间隔） |
| **角色原地左右扭，不打怪** | 这是已知未彻底解决的问题。`转向冷却` 已默认 1.5s。**已验证无效、别再试**：目标侧锁定、转向冷却调到 1.5s 以上、"侧占用去抖"（实测反而更差） |
| **满血也一直按药键，几秒后停任务** | `喝血阈值` 填成了 >1 的数，见雷区 1 |
| **任务莫名其妙停了** | 看 `logs/ok-script.log` 尾部 |
| **跑测试大批失败，且 0.3 秒就跑完** | `screenshots/test_frames/` 被 GUI 清掉了，从 `_frames_backup\` 复制回来 |
| **开着开着整机卡死/未响应** | 已在 commit `bd3cf61` 修掉（OCR 识别阶段按输入形状永久缓存编译内核，9 分钟能吃掉 8GB 内存）。确认你在最新版上 |

---

## 八、已知限制

- **只支持 2560x1440**，其他分辨率 ROI 全偏
- **只支持前台挂机**，后台/最小化不行
- 走位拾取默认关闭（掉平台风险）
- Buff 巡检未接入
- 药水数量 OCR 对小号描边数字会漏检（best-effort 跳过），药没了靠「连续喝药无效」兜底停机
- 「原地左右转向」问题未彻底解决
- 换新地图挂机效果不好的话，多半要**补数据重训 YOLO 模型**，见 [`OPERATION_GUIDE.md`](OPERATION_GUIDE.md) 第 1 节
